# Registro de auditorías — qa-flujos

> El agente `qa-flujos` agrega una entrada al terminar cada auditoría. Las más nuevas van arriba.
> Formato: fecha · flujo auditado · veredicto · hallazgos (con severidad) · qué quedó sin verificar.

<!-- Las entradas nuevas se agregan acá abajo, la más reciente primero. -->

## 2026-07-13 · MERCADO PAGO Slices 2 + 2.5 (cobro real de deudas por link) — lib/pagos.js (imputarPagoTx/revertirPagoTx), routes/webhooks.js (POST /mercadopago), routes/cargos.js (refactor cobrar-cuenta + link-pago), lib/mercadopago.js, modelo PagoMP — solo lectura · Veredicto: APTO CON RESERVAS (núcleo de plata correcto: RN-70/71/72/73/74 cumplidas, refactor idéntico, idempotencia real; PERO 2 huecos de "plata de más del jugador sin alerta" que un dueño puede reclamar — no pierden plata del club ni duplican deuda)

Archivos leídos completos: lib/pagos.js (imputarPagoTx :56-107, revertirPagoTx :123-146, cobrosEfectivoEntre/ingresosPorMetodoEntre :15-41), routes/webhooks.js (completo), lib/mercadopago.js (completo), routes/cargos.js (completo, foco cobrar-cuenta :224-285 + link-pago :358-423), lib/serializable.js, schema.prisma (PagoMP :569-587, Pago :532-548). Diff `git diff HEAD -- cargos.js` para comparar el refactor contra la versión previa inline.

VERIFICADO OK (traza real — lo que da confianza):
- REFACTOR cobrar-cuenta IDÉNTICO al previo: el diff confirma que la lógica FIFO inline se reemplazó 1:1 por imputarPagoTx. imputarPagoTx re-lee las deudas con el MISMO where (cargos: {id in, clubId, jugadorId, estado:'pendiente', comandaId:null}; reservas: {id in, clubId, jugadorId, pagado:false}), mismo orden FIFO (createdAt para cargo, fecha+horaInicio||createdAt para reserva), mismo metodoStamp (1 línea→método / varias→'mixto'), mismo shape de Pago+PagoLinea+imputaciones. En el camino mostrador montoACobrar ya viene validado ≤ totalRestante → aImputar===round(monto) → lineasEf===lineasIn → cap inactivo → comportamiento previo intacto. Split, parcial y caso simple preservados.
- RN-70 (acreditar SOLO por re-consulta): el webhook NUNCA usa el body para el estado/monto; obtenerPago(null, paymentId) re-consulta la API y de ahí salen status y transaction_amount (webhooks.js:115,132-136). La firma x-signature es soft (solo loguea) pero el candado real es la re-consulta. Correcto.
- RN-71 idempotencia mismo payment (secuencial Y concurrente): guard externo (webhooks.js:128 mpPaymentId===paymentId && status===mapStatus) + re-check DENTRO de runSerializable (webhooks.js:57-58 `if actual.status==='approved' && actual.pagoId return`). Dos webhooks concurrentes del mismo payment: ambos entran a la TX, SSI detecta rw-conflict sobre la fila pagoMP + cargo → aborta uno (P2034) → reintenta → ve pagoMP ya approved+pagoId → no-op. No duplica Pago ni sube saldoPagado dos veces. El retransmit del mismo payment sale por el guard externo.
- Reorder de notificaciones (approved viejo llega tras refund) NO re-cobra: como el estado se toma SIEMPRE de la re-consulta fresca (no del body), un approved retransmitido tras refund devuelve status vigente 'refunded' → no re-imputa. RN-70 cubre el reordenamiento.
- RN-72 (deuda al bruto): imputa transaction_amount (bruto pagado), sin restar comisión. saldoPagado sube por el bruto. La comisión MP no toca deuda ni la imputación.
- RN-73 (no ensucia efectivo): imputarPagoTx crea PagoLinea metodo='mercadopago'; cobrosEfectivoEntre filtra metodo:'efectivo' → lo ignora; ingresosPorMetodoEntre lo lista como 'mercadopago' aparte. Arqueo de efectivo intacto.
- RN-74 (refunded/charged_back reabre): procesarPago branch refunded/charged_back busca el Pago por pagoMP.pagoId y si no está anulado corre revertirPagoTx (reabre cargo pendiente, baja saldoPagado, marca Pago anulado) — todo en runSerializable. + warn. Correcto.
- Estados intermedios: in_process/authorized/pending→'pending' y rejected/cancelled→'rejected' caen al update final sin tocar plata (webhooks.js:96-97). binary_mode:true en la preferencia reduce los 'pending' raros. transición pending→approved del mismo payment funciona (guard 128 no matchea porque status difiere).
- Multi-tenant: el webhook es público pero scopea por pagoMP.clubId; imputarPagoTx busca el cargo/reserva con {id, clubId} → no cruza clubes. Un extRef es un cuid no adivinable + obtenerPago exige que el payment sea real y approved en la cuenta MP del club.
- RN-76/77 link-pago: expiraAt=+7d; reuso de link vivo por findFirst {origen,refId,status in [iniciado,pending], initPoint not null, no vencido} → no duplica links; PagoMP se crea PRIMERO (su id = external_reference) y si crearPreferencia falla marca status='error'. Multi-tenant por {id, clubId}. mpConfigurado gatea 503.
- RN-75 (cap sobrepago total): restante 0 → imputarPagoTx devuelve imputado:0 → procesarPago marca sobrepago + warn + statusDetail, pagoId=null. NO imputa de más ni duplica deuda.

HALLAZGOS:
- 🟠 [Seguro] SOBREPAGO PARCIAL: excedente que pagó el jugador de más NO se registra NI alerta. imputarPagoTx capa aImputar=min(round(amount), totalRestante) (pagos.js:73) y ajusta la única línea a lo imputado (:80) → la caja registra solo el restante, correcto para no duplicar deuda. PERO el flag de sobrepago del webhook solo se enciende si imputado===0 (webhooks.js:67); si imputado>0 (pagó de más pero algo se aplicó) NO hay warn, NO se marca statusDetail, y la diferencia amount−restante queda huérfana en MP sin rastro. Escenario REAL (el que menciona RN-75): deuda 1000 → se genera el link por 1000 → el admin cobra 600 en efectivo (restante baja a 400) → el jugador paga el link 1000 → imputa 400, sobran 600 pagados por MP que NO se registran ni avisan devolución. Riesgo frente al dueño: el jugador reclama "pagué 1000 por el link" y en el sistema solo figuran 400 acreditados + los 600 sin rastro → parece que el club se quedó con la diferencia. Sugerencia: cuando aImputar < round(amount) (hubo cap por cualquier motivo), devolver el excedente en el retorno de imputarPagoTx y que el webhook loguee warn + marque statusDetail 'sobrepago parcial $X' igual que hace con el total.
- 🟡 [Seguro] DOBLE PAGO DEL MISMO LINK (dos payments distintos, mismo PagoMP) → segundo pago ignorado en SILENCIO, sin alerta. Si el jugador paga el link dos veces, MP genera dos payments con distinto mpPaymentId pero el mismo external_reference (=pagoMP.id). Primer webhook: imputa, deuda saldada, pagoMP.pagoId=P1. Segundo webhook (payment2): guard externo (webhooks.js:128) no matchea porque mpPaymentId difiere → entra a procesarPago → re-check `actual.status==='approved' && actual.pagoId` TRUE → return no-op SILENCIOSO (webhooks.js:58, sin warn). Correcto que NO duplique la imputación de la deuda (bien), pero el segundo pago real quedó sin registro NI alerta de devolución: el club recibió doble en MP y el sistema no lo refleja. El @unique(mpPaymentId) no se viola porque el segundo hace no-op antes de escribir. Riesgo: jugador pagó dos veces, sin rastro para devolverle → reclamo. Sugerencia: en el re-check no-op, si el paymentId entrante ≠ pagoMP.mpPaymentId y ambos serían approved, loguear warn de "pago duplicado del mismo link, revisar devolución" (misma clase que el 🟠 anterior).
- 🔵 [Seguro] procesarPago asume origen='cargo' para resolver el jugadorId (webhooks.js:59 `tx.cargo.findUnique({where:{id: pagoMP.refId}})`). Si algún día se genera un PagoMP con origen='reserva' (el modelo lo admite; hoy link-pago solo crea 'cargo', cargos.js:390), ese findUnique da null → el Pago se crea con jugadorId=null (imputa igual porque sin jugadorId no filtra). Latente, no activo hoy. Sugerencia: resolver jugadorId según origen (reserva.jugadorId si origen==='reserva').
- 🔵 [Suposición] obtenerPago(null, paymentId) usa el token de MP_ACCESS_TOKEN de env (single-club). Hoy 1 club → OK. En el futuro OAuth multi-tenant, un webhook con extRef de otro club se consultaría con el token equivocado. No rompe hoy. Ya documentado en el propio código (mercadopago.js:4-6).

QUÉ NO PUDE VERIFICAR (límites honestos): no corrí el backend ni pegué webhooks reales con curl (cierre por trazado + análisis de aislamiento SSI, no reproducido con dos requests concurrentes reales); no verifiqué el comportamiento REAL de la API de MP (si transaction_amount puede venir < unit_price por cupones, o con centavos) — el análisis es sobre el código que recibe ese amount; no leí el frontend que dispara link-pago ni la pantalla de éxito (fuera del alcance del backend de plata); asumí que PagoLinea/normalizarMetodo mantienen el shape ya visto en auditorías previas; no confirmé que MP_WEBHOOK_SECRET esté seteado en el deploy (la firma es soft y el candado es la re-consulta, así que no bloquea la correctitud).

## 2026-07-10 · CAZA DE "GUARDADO FANTASMA" (pre-deploy, primer cliente) — flujos operativos: reservas, turnos fijos, pagos/caja, jugadores, stock, gastos — solo lectura · Veredicto: NO APTO PARA DEPLOY hasta cerrar el 🟠 de la reserva del jugador (1 🟠 que pierde reservas + 1 🟡 config sin verificar respuesta + varios 🔵)

Clase de bug buscada: acción que PARECE guardar (cambia UI / cierra form / muestra OK) pero (a) solo toca estado local y no persiste; (b) pega al backend pero no chequea res.ok; (c) falla y se traga el error en silencio.

Archivos leídos: lib/api.js (wrapper request), store/clubStore.js (saveClub/saveConfig), store/reservasAdminStore.js, store/turnosFijosStore.js, store/reservasStore.js (por uso), pages/PlayerReservasPage.jsx (handleConfirmar :478-576, carga canchasDB :232-236, canchaDBId :320-324), pages/PlayerTurnosFijosPage.jsx (ausencia/baja/retirar), pages/PlayerMisReservasPage.jsx (handleCancelar :257-275), pages/ReservasPage.jsx (handleSave :3104, handleCancelar :3179-3250, handlePago :3252-3270, abrirCheckout, reservasDia :3082), pages/JugadoresAdminPage.jsx (alta/edición/baja por grep), pages/PagosPage.jsx (guardarMetodos/guardarVistaJugador :780-802), pages/QuienesSomosPage.jsx (TabCanchas guardarCancha :956 + handleSave :1014).

HALLAZGOS:
- 🟠 ALTO [Seguro] tipo (a) — La reserva del jugador se puede "guardar" SOLO en localStorage sin tocar el backend, mostrando pantalla de éxito. PlayerReservasPage handleConfirmar envuelve el POST /reservas (y POST /turnos-fijos) en `if (canchaDBId && token)` (:522,:488). `canchaDBId` se resuelve por coincidencia de NOMBRE contra `canchasDB` (:322), y `canchasDB` se carga con `api.get('/canchas?clubId=...').catch(()=>{})` (:232-236, catch vacío que deja `canchasDB=[]`). Si esa GET falla (cold start de Railway tras idle, blip de red, auth) → `canchaDBId=null` para TODAS las canchas → el POST se SALTEA, pero `addReserva` (reservasStore→localStorage) corre igual (:549) y se muestra `exitoReserva` con "Reserva confirmada". El jugador cree que reservó; en el backend no existe nada; el admin nunca lo ve; al refrescar se pierde; y el slot sigue libre → otro jugador lo reserva de verdad = conflicto en cancha por un falso OK. Mismo patrón para el turno fijo (:485-517). Repro: cortar el backend un segundo (o forzar 500 en /canchas) al abrir "Reservar cancha", luego reservar → sale "confirmada" sin fila en DB. Riesgo frente al dueño: el jugador llega al club con "su" reserva y la cancha está ocupada/no figura — el peor golpe de confianza posible. Fix sugerido: si `canchaDBId`/token faltan, NO mostrar éxito — tirar error visible; y no dejar que la GET /canchas caiga a array vacío en silencio (reintentar/bloquear el submit hasta tener canchasDB). Sacar el `addReserva` a localStorage como "fuente" de la reserva.
- 🟡 MEDIO [Seguro] tipo (b) — `saveConfig` del clubStore nunca chequea `res.ok`. clubStore.saveConfig (:182-189) hace `await fetch(...PATCH /clubs/me...)` y setea `_dirty:false` sin mirar el status; `fetch` no rechaza en 4xx/5xx. Los callers PagosPage.guardarMetodos (:783-786) y guardarVistaJugador (:795-798) hacen `await saveConfig(token)` dentro de try y muestran "Métodos de cobro actualizados" / "El jugador verá su resumen" aunque el backend haya devuelto 403/500. Además `if (!token) return` sale sin throw → también falsea éxito. Contraste directo: el hermano `saveClub` (:193-232) SÍ chequea `if(!configRes.ok||!canchasRes.ok) return false` y devuelve booleano. Persiste: métodos de cobro aceptados y comisión MP por método (afecta caja) + flag mostrarConsumoJugador. Repro: forzar 500 en PATCH /clubs/me al guardar métodos → toast de éxito, al refrescar vuelve atrás. Fix sugerido: que saveConfig chequee res.ok y devuelva bool/throw, y que los callers avisen si no persistió (como ya hace TabCanchas con saveClub).
- 🔵 BAJO [Seguro] tipo (b) — QuienesSomos TabCanchas.handleSave (:1014-1018, guardado global de horarios) llama `saveClub()` sin mirar el `ok` que devuelve y setea `saved=true` (check "Guardado") pase lo que pase. Los horarios rigen la disponibilidad de reservas. Contraste con guardarCancha (:956-961) del MISMO componente, que sí chequea ok. Fix: chequear el retorno de saveClub también acá. (Varios otros tabs de config de landing —info/hero/historia/galería/etc.— repiten el `saveClub()` sin chequear, fuera de la zona operativa pero mismo patrón.)
- 🔵 BAJO [Seguro] tipo (c) — PlayerTurnosFijosPage.handleRetirarSolicitud (:306-315) tiene `catch { /* fallback silencioso */ }`: si el DELETE del turno fijo pendiente falla, no hay toast ni cambio de estado. No es guardado fantasma (el store NO se muta si el await tira, y el poll de 30s reconcilia), pero deja al usuario sin feedback. Fix: toast.error en el catch.
- 🔵 BAJO [Probable] tipo (a) — Ramas LOCAL-ONLY legacy en la grilla admin (ReservasPage): handleCancelar rama `player_` (:3201-3216, cancela vía reservasStore sin backend + toast "cancelada"), rama final `deleteReservaAdmin(id)` (:3247) y handlePago `else → pagarReservaAdmin(id)` (:3268, marca pagado solo en store local). Trazado: la grilla se arma desde `reservasBackend` (ids `backend_`) + `turnosFijosDia` (ids `fijo_player_`) — reservasDia :3082-3085 mete además `reservas` (reservasAdminStore) pero `handleSave` postea TODO a /reservas/admin (backend, :3104-3117) y no vi camino vivo que llame addReservaAdmin, así que esas ramas quedan sin alimentarse (dead code). Si se reactivaran o quedara data vieja en localStorage, serían guardados/pagos fantasma. Fix: borrar las ramas y los métodos local-only del store (addReserva/deleteReserva/pagarReserva/updateReserva de reservasAdminStore; addTurnoFijo/liberarTurno/ausentarDia de turnosFijosStore) para que no vuelvan a engancharse por error.

VERIFICADO OK (traza real, sin fantasma):
- lib/api.js `request` tira Error en TODO res no-ok (:11-39) con .status/.code/.data → cualquier flujo que use `api.*` (la enorme mayoría: JugadoresAdminPage alta/edición/baja, PlayerMisReservas cancelar, ReservasPage handleSave/handleAprobar/handleCancelar backend_/handlePago backend_/handleGuardarEdicion, PagosPage cobros, CheckoutTurno, tabs de pagos) NO puede mostrar éxito con backend caído: el await salta al catch. Tipo (b) cubierto donde se usa api.
- PlayerReservasPage happy path (canchaDBId+token presentes): POST real, y el catch (:507,:543) setea errorReserva + return ANTES de mostrar éxito → sin fantasma cuando las canchas cargaron.
- PlayerMisReservasPage.handleCancelar (:257-275): DELETE real primero, recién después toca store y muestra toast según res.cargoAplicado; catch con toast.error. Correcto.
- PlayerTurnosFijosPage: cancelar (:290-304), ausencia (:317-348) pegan al backend, mutan store con la respuesta y en catch muestran el error real (no fingen éxito). Guards submitting/cancelando/enviando + disabled OK.
- JugadoresAdminPage: alta (:107-113), edición (:269-274), baja (:922-946) vía api con guard `if(submitting) return` + disabled + catch que muestra error. Baja con deuda → 409 sube por api.throw al catch. Sin fantasma.
- ReservasPage.handlePago rama backend_ (:3252-3266): update optimista + PATCH real + REVIERTE al estado previo si el backend falla (catch :3263-3265). Bien resuelto.
- clubStore.saveClub (:193-232): chequea res.ok de config Y canchas, devuelve bool, catch→false; TabCanchas.guardarCancha lo consume y avisa con toast.error si no persistió (el fix del bug original de canchas, verificado cerrado).

QUÉ NO PUDE VERIFICAR (límites honestos): no corrí el backend ni reproduje con curl la falla de GET /canchas (el 🟠 es por trazado del guard + catch vacío, no reproducido en vivo); no leí GastosTab/StockTab/CajaTab/VentasTab línea a línea en esta pasada (los di por buenos apoyándome en la auditoría 2026-06-28 que los marcó con setSaving+disabled+éxito-tras-await y en que usan `api` que tira en no-ok — no re-verifiqué que no haya aparecido un store-only nuevo); no confirmé si `addReservaAdmin` se invoca en algún handler que no leí de ReservasPage (asumo dead por handleSave→backend, pero el archivo es de 3000+ líneas y solo leí las zonas de mutación); no verifiqué si reservasStore (localStorage del jugador) se sigue leyendo como fuente en PlayerDashboard además del backend.

## 2026-07-10 · ASISTENTE IA "WIarky" — modos de falla funcionales de sus ACCIONES (crear reserva/jugador/convocatoria/gasto/ascenso/abrir caja) — solo lectura · Veredicto: NO APTO SIN SUPERVISIÓN (arquitectura de escritura sólida — nada se crea sin botón + backend blindado — pero 1 🟠 de plata invisible + 3 🟡 de datos/scheduling que un dueño real dispararía en el uso normal)

Archivos leídos completos: backend lib/insight.js (WIARK_TOOLS + ejecutarHerramientaWiark + responderChatAgente + red anti-fantasma), routes/clubs.js (/me/insight/chat :330, /me/insight/accion :353-422), lib/convocatorias.js (organizarConvocatoria + crearConvocatoriaCompleta), lib/deudas.js (turnosImpagosDeuda), routes/reservas.js (POST /admin :669-780), routes/jugadores.js (POST / :981-1022); frontend components/asistente/AsistenteWiark.jsx (ConfirmAccion + enviar + card proactiva abrir_caja), pages/ReservasPage.jsx (handleSave :3104-3117, contraste de payload de la grilla).

ARQUITECTURA SÓLIDA (verificado, es lo que salva a WIarky):
- WIarky NUNCA escribe desde el chat: las tools crear_* solo PREPARAN un `artefacto {tipo:'confirmacion', accion, datos}`. La escritura real ocurre recién al tocar el botón en ConfirmAccion (front), que pega a un endpoint real. Un "confirmado fantasma" en el texto tiene CERO efecto en DB. Además hay red anti-mentira determinística (insight.js:748-754): si el texto afirma "creado/reservé/confirmado" y no hubo artefacto con .accion, se fuerza re-turno.
- Escritura ruteada a endpoints ya blindados: crear_reserva→POST /reservas/admin (runSerializable + anti-doble-booking + anti-TF-conflicto, :695-780); crear_jugador→POST /jugadores (unique clubId_dni → 409, :988-993); crear_convocatoria→crearConvocatoriaCompleta→organizarConvocatoria (runSerializable, reserva N canchas atómico, 409 si <N libres, categorias normalizadas :32,:72). TOCTOU prepare→confirm cubierto: el backend re-valida en la TX Serializable → 409 si el slot se ocupó entre medio.
- Doble-creación contenida: ConfirmAccion tiene guard `if (estado!=='idle') return` + disabled durante 'cargando'; y aunque aparezcan 2 cards del mismo acto (loop de tools o red anti-fantasma), la 2da confirmación choca contra Serializable/unique → 409 (segundo no duplica). crear_jugador dedupe por DNI en prepare (insight.js:641) y en el POST.
- Multi-tenant: todas las tools y accion usan req.user.clubId; buscar_jugador/gatherDisponibilidad/crear_reserva/crear_jugador scopean por clubId. /me/insight/chat y /accion con requireAuth+requireRole('admin')+requireOwner. Sin fuga entre clubes en el flujo normal.
- Slots 1.5h y cross-midnight OK en crear_reserva: horaFin = (toMin(horaInicio)+90)%1440 (insight.js:605-607) → 23:30 da '01:00' correcto; y solo deja crear si horaInicio ∈ franjas reales del club (cDisp.horas de franjaTimes, cross-midnight aware). No genera slots off-grid ni +1h.
- crear_convocatoria: mín 2 canchas forzado en prepare (Super8=2 fijo; Americano Math.max(2,...) :503), verificación DURA de 2+ canchas a la misma hora ANTES de ofrecer el botón (:507-515). Género/categoría/visibilidad se preguntan explícitamente (no se asumen). Categorías → normalizarCategoria (canónico) en convocatorias.js. Fecha pasada rechazada en prepare y en organizarConvocatoria (:35).

HALLAZGOS (priorizados):
- 🟠 ALTO [Seguro] crear_reserva genera turnos SIN PRECIO → invisibles al sistema de deudas/cobros. La tool solo manda `precio` si el dueño lo dicta explícito (insight.js:613,:628); si no, va null. /reservas/admin guarda `precio: precio ? ... : null` (reservas.js:733). CONTRASTE: la grilla admin SIEMPRE manda `precio: nueva.monto` (prefill del precio de la cancha, ReservasPage:3113). Y turnosImpagosDeuda EXIGE `(r.precio ?? 0) > 0` (deudas.js:26) → un turno creado por WIarky, jugado y no pagado, NUNCA aparece en deudas, ni en consultar_deudores, ni como cobro pendiente. Peor: crear_reserva vincula jugadorId SOLO con match EXACTO nombre+apellido y con un `norm` que NO saca acentos (insight.js:619, distinto del resto que usa NFD) → "reservá para Juan"/"para Julián" casi nunca matchea → jugadorId null; y turnosImpagosDeuda además exige `jugadorId: {not:null}`. Resultado del caso común ("WIarky, reservá la 1 a las 20 para Juan"): turno con precio null + jugadorId null = plata que se juega y desaparece del control de deuda. Riesgo frente al dueño: ingresos que se le escapan sin rastro, justo lo contrario de lo que vende el asistente. Sugerencia: default precio = cancha.precioTurno cuando no se dicta, y usar el mismo matcher con NFD/parcial que crear_convocatoria/buscar_jugador.
- 🟡 MEDIO [Seguro] crear_jugador guarda categoría en formato NO canónico → no matchea convocatorias/torneos. La tool toma `input.categoria.trim()` crudo (insight.js:643) y POST /jugadores lo persiste tal cual (`categoria: categoria ?? null`, jugadores.js:1003) SIN normalizarCategoria. Si el dueño dice "6ta"/"sexta"/"6", queda "6ta" en vez de "6ta Categoría". Es EXACTAMENTE el bug conocido "4ta" vs "4ta Categoría": ese jugador no entra en `categoria:{in:categorias}` de crearConvocatoriaCompleta (que sí normaliza el filtro, convocatorias.js:78) → no lo notifica; y desalinea filtros de torneos/ascensos. Contraste interno: convocatorias normaliza, crear_jugador no. Sugerencia: pasar categoria por normalizarCategoria en la tool o en POST /jugadores.
- 🟡 MEDIO [Probable] Americano: `cupos` y `canchas` no se reconcilian → convocatoria con canchas insuficientes para el cupo. La tool toma `cupoMax` de input.cupos y `canchas` de input.canchas de forma INDEPENDIENTE (insight.js:503,:533); NO deriva canchas=cupos÷4. El prompt le pide al modelo que calcule canchas=cupos÷4 (líneas 697), pero Haiku es flojo calculando (memoria del proyecto) y `canchas` default = 2. Caso: Americano de 16 cupos donde el modelo omite `canchas` → se verifican/reservan solo 2 canchas (insight.js:503, disponibilidad chequea `>=2`), se crea convocatoria cupoMax=16 sobre 2 canchas. El fixture (armarFixtureConvocatoria, no releído en esta pasada) queda con 16 anotados y 2 canchas → scheduling inválido/sobrecupo de cancha cuando se llene. El accion endpoint tampoco valida (clubs.js:373 solo exige cupoMax>=2, no cupos<=canchas*4). Se manifiesta al armar fixture, no al crear. Sugerencia: derivar canchas=ceil(cupos/4) en la tool para Americano, o validar cupoMax<=canchas*4 en prepare y en /accion.
- 🟡 MEDIO [Seguro] crear_reserva con 2+ homónimos: reserva desvinculada SIN aviso. En insight.js:618-624, si hay >1 match exacto, no entra ni al branch de match (jugadorId queda null) ni al de "no registrado" (nota queda ''): la reserva se crea con el nombre suelto, sin jugadorId y SIN avisarle al dueño que había ambigüedad (a diferencia de buscar_jugador/crear_convocatoria, que sí piden aclarar cuál). El turno queda desvinculado del jugador (suma al 🟠 de plata invisible). Sugerencia: replicar el manejo multi-match (pedir apellido) como en las otras tools.
- 🔵 BAJO [Seguro] /me/insight/accion crear_convocatoria acepta `canchas=1` (clubs.js:376 `Math.max(1,...)`), violando "mín 2 canchas" a nivel endpoint. En el flujo normal datos.canchas siempre llega >=2 (prepare lo fuerza), así que solo es alcanzable con cliente manipulado. Defense-in-depth. Sugerencia: `Math.max(2,...)` en el endpoint.
- 🔵 BAJO [Probable] abrir_caja: check-then-create sin transacción/Serializable (clubs.js:410-414: findFirst estado:'abierta' → create, sin unique en arqueoCaja). Dos requests concurrentes (dos pestañas / doble render de la card proactiva) podrían abrir dos cajas. Prob. baja (un dueño, guard de card, backend re-chequea). Sugerencia: unique parcial (clubId, estado='abierta') o runSerializable.
- 🔵 BAJO [Suposición] /accion no revalida que `organizadorJugadorId` (convocatoria) ni `jugadorId` (reserva, vía /reservas/admin) pertenezcan al clubId antes de escribir el FK. En flujo normal se resuelven contra jugadores del propio club en prepare; solo un cliente manipulado inyectaría un cuid foráneo, y el efecto es un FK malo en el PROPIO club (no fuga desde otro). Defense-in-depth.

VERIFICADO OK (traza real): red anti-"confirmado fantasma" (insight.js:748); nada se escribe desde el chat (solo prepara artefacto); Serializable + anti-doble-booking en los 3 caminos de escritura reales; TOCTOU prepare→confirm cubierto por re-validación en la TX; unique DNI en crear_jugador (prepare + POST); dedupe DNI; multi-tenant por req.user.clubId en tools/accion/endpoints; slots 1.5h y cross-midnight correctos en crear_reserva (mod 1440, franjas reales); mín 2 canchas + verificación dura de disponibilidad + fecha pasada rechazada en crear_convocatoria; categorías de convocatoria normalizadas; género/categoría/visibilidad preguntados (no asumidos); ascender_jugador valida dirección de ascenso (nivelDeCategoria) + scope clubId + normaliza destino; abrir_caja idempotente por 409 en flujo secuencial; guard de doble-click en ConfirmAccion.

QUÉ NO PUDE VERIFICAR (límites honestos): no corrí el backend ni pegué con curl (cierre por trazado); no leí el cuerpo de armarFixtureConvocatoria → el impacto exacto del mismatch cupos/canchas (🟡 #3) lo infiero del modelo de datos, no reproducido; no reproduje comportamiento REAL del LLM Haiku (si pasa o no `canchas` y `precio` correctos, si dispara la tool 1 o 2 veces) — los 🟡/🟠 son sobre el CÓDIGO que recibe esos inputs, no sobre la política del modelo; no releí normalizarCategoria ni nivelDeCategoria en esta pasada (asumidos por firma, vistos en auditorías previas); no verifiqué el prefill del monto en el modal de la grilla (asumido por `precio: nueva.monto`).

Veredicto: NO APTO para que un dueño lo use SIN SUPERVISIÓN. La arquitectura de escritura está bien pensada (confirmación humana obligatoria + endpoints Serializable + anti-fantasma) y NO hay doble-booking, doble-cobro ni fuga de tenant. PERO el asistente introduce datos degradados en el uso normal: reservas sin precio ni jugador vinculado que se vuelven plata invisible (🟠), jugadores con categoría no canónica que rompen el matching (🟡), y convocatorias Americano con canchas insuficientes para el cupo (🟡). Un dueño confiando a ciegas perdería control de deuda y ensuciaría su base. APTO CON SUPERVISIÓN (el dueño revisa cada botón y completa precio/categoría a mano) mientras no se cierren el 🟠 y los 🟡.

## 2026-07-09 · BLOQUE "entrega parcial + split de métodos" (pago-parcial-split): lib/pagos.js + cargos.js (cobrar-cuenta/estado/DELETE) + caja.js + deudas.js + reservas.js (PATCH /pago, /cobrar, /cuenta) + pagos.js — solo lectura · Veredicto INICIAL: NO APTO (2 🟠 doble-conteo) → tras fixes del coordinador el MISMO día: APTO (los 4 hallazgos cerrados, re-verificados por código; ver RE-CHECK abajo)

Archivos leídos completos: lib/pagos.js, routes/cargos.js, routes/caja.js, lib/deudas.js, routes/pagos.js, schema.prisma (Reserva/Cargo/Pago/PagoLinea), reservas.js (PATCH /:id/pago :947-991, /:id/cobrar :997-1077, GET /:id/cuenta, POST /:id/cuenta :1130-1229), frontend PagosPage.jsx (cobrarTodo :454-488, anular :822-847, cambiarMetodo, guards disabled={saving}).

RE-CHECK (mismo día, tras fixes del coordinador) → los 4 hallazgos CERRADOS, veredicto revisado a APTO:
- 🟠 A CERRADO: cobrar-cuenta ahora hace lectura de saldos (tx.findMany) + FIFO + tx.pago.create + updates DENTRO de runSerializable (cargos.js:241-300), re-leyendo saldoPagado adentro. Import ok (:8). Errores con err.status (:304). Dos cobros del mismo ítem → P2034 aborta uno, reintenta, un solo Pago.
- 🟠 B CERRADO: /cobrar bloquea 409 en_libro_plata si cobrarTurno && reserva.saldoPagado>0 (reservas.js:1014); /cuenta suma el parcial del libro al guard: pagadoDelTurno = r.pagado?precioT:(r.saldoPagado??0) en yaCubierto (:1190,1195-1196). Verificado que no reintroduce doble conteo (ledger 500 + cargo restante 500 = 1000; reserva no entra a caja con pagado false).
- 🟡 C CERRADO: anular usa runSerializable + re-chequea anuladoAt fresco dentro de la TX (pagos.js:43-47); des-pago legacy reserva (reservas.js:975) y reopen cargo (cargos.js:362) migrados a runSerializable. Reversas concurrentes sobre el mismo ítem colisionan en la fila bajo Serializable → reintento con saldo fresco, sin lost-update (depende de retry P2034 de serializable.js, ya confirmado).
- 🔵 D CERRADO: if (saving) return al inicio de cobrarTodo (PagosPage:455) y anular (:824).

HALLAZGOS (priorizados):
- 🟠 [Seguro] cobrar-cuenta SIN runSerializable ni idempotencia → doble Pago = doble conteo en caja. cargos.js:222-319: las deudas se leen FUERA de transacción (:238-241), se calcula el saldo nuevo en JS y recién al final `prisma.$transaction([create Pago, ...updates])` (:304) con aislamiento default (ReadCommitted). Los updates hacen SET del saldoPagado a un valor absoluto derivado de la lectura vieja (no increment). Dos cobros concurrentes del MISMO ítem (o un doble-submit que evada el `disabled={saving}`): ambos leen saldo=0, ambos crean un Pago con su PagoLinea (no hay unique en Pago), ambos setean saldoPagado=1000. La caja (`ingresosPorMetodoEntre`) suma las DOS PagoLinea → 2000 por una deuda de 1000. El arqueo queda inflado → el efectivo físico da FALTANTE → sospecha de robo al empleado. Mitigante en single-user: `disabled={saving}` + setSaving en cobrarTodo (PagosPage :655,:462). Exposición real: dos empleados/cajas cobrando el mismo jugador en paralelo (el proyecto YA tiene RBAC multi-empleado) o un retry de red — ahí sube a 🔴. Contraste: todos los demás caminos de cobro son o Serializable (/cuenta, /cobrar) o idempotentes por `updateMany where acotado`; este es el único no-idempotente que además CREA un registro nuevo por request. Sugerencia: envolver la lectura+imputación+create en runSerializable (re-leer saldoPagado dentro de la TX), o un unique/guard anti-duplicado.
- 🟠 [Seguro] El guard anti-sobrecobro de la grilla (POST /cuenta y /cobrar) IGNORA reserva.saldoPagado del libro nuevo → un turno cobrado PARCIAL por Cobranzas se re-cobra ENTERO por la grilla. reservas.js:1188-1190: `yaCubierto = (r.pagado?precio:0) + Σ cargos tipo 'reserva'`. El pago parcial vía cobrar-cuenta NO crea cargo tipo reserva: sube reserva.saldoPagado directamente (cargos.js:297-300) y deja pagado=false, cobroOmitido=false. Secuencia real: admin cobra 500 de un turno de 1000 en Cobranzas (queda saldoPagado=500, restante 500) → abre la grilla, el turno figura impago (la grilla lee reserva.pagado, no saldoPagado) → "Cobrar turno" por 1000 → el guard ve yaCubierto=0 y lo permite, crea cargo tipo reserva 1000 pagado + cobroOmitido=true. Caja cuenta 500 (PagoLinea) + 1000 (cargo legacy) = 1500 por un turno de 1000, y los 500 del libro quedan huérfanos. Doble-cobro cross-UI. Sugerencia: sumar `reserva.saldoPagado` a `yaCubierto`, y/o bloquear en /cuenta,/cobrar si saldoPagado>0 (como ya hace PATCH /:id/pago con el 409 en_libro_plata).
- 🟡 [Probable] Anular / des-pago legacy revierten con SET desde lectura stale, sin serializable → dos reversas concurrentes sobre el mismo ítem pierden update. revertirPagoTx (pagos.js:63-86) hace `nuevoSaldo = max(0, saldo - imp.monto)` con `saldo` leído dentro de la tx pero SIN aislamiento serializable (pagos.js:41 y cargos.js:373 / reservas.js:975 usan `prisma.$transaction(async…)` = ReadCommitted). Caso: un ítem (cargo 1000) con Pago A(400) + Pago B(600). Anular A y anular B en paralelo: ambos leen saldo=1000; A setea 600, B setea 400; gana el último → queda saldoPagado=400 (o 600) con AMBOS pagos anulados, cuando debería ser 0. La caja queda bien (ambas PagoLinea anuladas, no cuentan) PERO el ítem queda con saldo residual sin pago que lo respalde: la deuda cobrable se sub-cuenta (el club "pierde" 400 de deuda) y DELETE queda bloqueado (saldo>0). Poco frecuente (requiere anular dos pagos del mismo ítem a la vez). El doble-anular del MISMO pago sí está cubierto (guard `anuladoAt` :40 + floor max(0,...)). Sugerencia: runSerializable en anular y en las reversas legacy, o increment/decrement atómico en vez de set.
- 🔵 [Seguro] Handlers cobrarTodo (PagosPage:454) y anular (:822) no tienen guard `if (saving) return` al inicio: dependen solo de `disabled={saving}`. Un doble-click ultrarrápido antes del re-render de React puede colar dos requests (y ahí pega el 🟠 A). El resto del proyecto usa disabled+guard. Bajo, pero es justo la superficie que abre el 🟠 A a un solo usuario.

VERIFICADO OK (traza real):
- Discriminador saldoPagado>0 SIN doble conteo en el flujo SECUENCIAL: caja legacy exige `saldoPagado:0` (pagos.js:19,20,32,33; caja.js:24,25) y el libro exige PagoLinea de Pago no anulado → un ítem está en UN solo libro. No hay forma de que un ítem con saldoPagado>0 caiga también en la lectura legacy. Los 409 `en_libro_plata` (reservas.js:961, cargos.js:361) impiden pagar por vía legacy un ítem que ya tiene saldo en el libro. DELETE cargo bloquea con saldoPagado>0 (cargos.js:328).
- FIFO sin sobre-cobro ni residuo: montoACobrar validado `<=totalRestante` (cargos.js:262) y `>0` (:259); split validado `Σlineas === montoACobrar` (:274); imputación reparte a las deudas más viejas y como monto<=totalRestante nunca sobra `resto`. Enteros (Math.round). Sin residuos.
- Cross-midnight NO reintroducido: la plata se lee por `pagadoAt` (timestamp real) con rangoDiaArg (caja.js:14); deudas.js:25 usa `finEnMin(horaInicio,horaFin)` (helper correcto, no el antipatrón `==='00:00'?1440`). El bloque no hace aritmética de horaFin.
- Multi-tenant BLINDADO: cobrar-cuenta valida jugador.clubId (:233) y lee items `where {clubId, jugadorId}` (:239-240); anular chequea pago.clubId===req.user.clubId (pagos.js:39); todos los reads de caja/ingresos scopean por clubId (relación pago.clubId incluida). revertirPagoTx busca ítems por id sin clubId pero el Pago ya está scopeado y sus imputaciones son de ítems propios (creados con filtro clubId) → sin fuga.
- deudas.js/cargoADeuda con parcial: `monto` = restante (total − saldoPagado), + montoOriginal + saldoPagado expuestos (deudas.js:29-37, cargos.js:22-33). Baja de jugador con deuda sigue bloqueada (un parcial deja estado pendiente / restante>0). Correcto.
- Reversa consistente en el flujo secuencial: revertirPagoTx floorea a max(0,…), reabre estado/pagado sólo si queda por debajo del total, marca anuladoAt sin borrar (auditoría). Anular de un pago combinado devuelve 409 pago_combinado con pagoIds y el front cascada-anula los pagos completos (PagosPage:834-841). El 409 en_libro_plata frena las vías legacy sobre ítems ya en el libro.

QUÉ NO PUDE VERIFICAR (límites honestos): no corrí el backend ni reproduje la concurrencia con curl (cierre por trazado); los 🟠/🟡 de carrera son por análisis de aislamiento (no reproducidos con dos requests reales); no leí snapshotProductos/descontarStock/normalizarMetodo en esta pasada (asumidos por firma, ya vistos en auditorías previas); no confirmé si el front de la grilla (ReservasPage/CheckoutTurno) llega a ofrecer "Cobrar turno" sobre un turno ya parcial-en-libro — el 🟠 B lo tracé por el guard backend, que es la última línea y hoy no contempla saldoPagado.

## 2026-07-05 · MÓDULO FINANCIERO / "Dirección del club" (motor finanzas.js + rutas + CostosPanel + DireccionPage) — solo lectura · Veredicto: APTO CON RESERVAS (ningún bloqueante de plata ni fuga de tenant; hallazgos de precisión/cosmética)

Archivos leídos completos: backend lib/finanzas.js, lib/finanzas.test.js, routes/finanzas.js, routes/costos.js, routes/eventos.js, app.js (montaje :77-93); frontend pages/DireccionPage.jsx, features/direccion/CostosPanel.jsx, lib/telemetria.js.

SÓLIDO (verificado):
- Multi-tenant BLINDADO: finanzas y costos montados con requireAuth+requireRole('admin')+requireFeature('direccion')+requirePermiso('caja') (app.js:80-81). Los 4 endpoints de finanzas usan req.user.clubId (finanzas.js:12,23,34,45). costos CRUD: GET/POST con clubId de req.user; PATCH/DELETE con updateMany/deleteMany + {id,clubId} (costos.js:75,88) → no se edita/borra costo de otro club. Cero query param clubId. Sin fuga.
- Cross-midnight CORRECTO en franjasDia/franjaTimes (finanzas.js:16-37): patrón `cierre==='00:00'?1440` PARA EL CIERRE del club (válido) + `if (ci<=ap) ci+=1440` que cubre 00:30/01:00/02:00. Los tests (finanzas.test.js:24-29,44-47) verifican exactamente eso (00:30=11, 01:00=11, 22:00→02:00=2, franjaTimes envuelve a 00:30 con mm%1440). Los tests cubren lo que dicen.
- Div-by-zero / NaN: todos los denominadores con guard (turnosDisponibles>0 :141,143,144; turnosPagados>0 :119,153; precioLista>0 :153; ingresoTotal>0 :262; vencidas>0 :136; d>0 en heatmap :414). breakEvenTurnos=null cuando fijo<=0 o contrib<=0 (:140). money(n??0) en front. Estados vacíos degradan a onboarding sin romper.
- Flujo de caja NO doble-cuenta TF materializados: reservasFuturas filtra esTurnoFijo:false (:309) → la proyección de TF (ocurrenciasDia×precio) y las reservas agendadas no se pisan.
- Yield descompuesto en 3 (hacés+fugaVacío+fugaDescuento) construido para sumar 100 (fugaDescuento=100−yield−fugaVacío, :159) con clamp; ratioPrecio clampeado a 1 (:155) → cobrar > lista no cuenta como fuga.
- Doble-submit OK: DireccionPage.guardar (guardando+disabled :399) y CostosPanel.guardar (saving+disabled :161). Sin localStorage de negocio (telemetria solo LEE token :17). Ausentismo etiquetado explícitamente como proxy de no-show (:129-131, front :483).

HALLAZGOS (ninguno bloqueante):
- 🟡 [Seguro] ocupacionPct y breakEvenPct SIN clamp (finanzas.js:144-145): si turnosVendidos > turnosDisponibles (reservas en días marcados inactivo, fuera de horario, o horario reducido después de crear la reserva) → ocupación puede salir >100% ("150% de ocupación" en el sub del RevPACH, DireccionPage:458). El heatmap SÍ clampea (Math.min(100,pct) :415) y turnosVacios usa max(0,...) — inconsistente. Número que "miente" al dueño. Sugerencia: Math.min(100, ...) en ocupacionPct/breakEvenPct.
- 🟡 [Seguro] Flujo de caja sobrestima el MES EN CURSO (finanzas.js:319-329): ocurrenciasDia cuenta TODAS las caídas del día de semana en el mes, incluidas las YA PASADAS del mes en curso. El "flujo a 90 días" es forward, pero el primer mes suma cobros de TF de fechas que ya ocurrieron (y posiblemente ya cobradas). La barra "Cobrás" del mes 1 queda inflada. Documentado como "aproximado" (:294) pero la sobrestimación del mes actual es concreta. Sugerencia: para k=0, contar solo ocurrencias con fecha >= hoy.
- 🟡 [Probable] Heatmap cross-midnight desalinea celdas nocturnas (finanzas.js:387-401): la disponibilidad de franjas post-medianoche se atribuye al día de APERTURA (franjaTimes de Lunes 22:00→02:00 mete '01:00' en Lunes), pero una reserva de ese slot se datea con el día CALENDARIO (martes). → la celda Lunes 01:00 muestra 0% (siempre fría) y la vendida cae en martes con disp posiblemente 0 (gris "cerrado"). Solo afecta clubes que cierran pasada la medianoche y SOLO el heatmap (no la plata: el agregado de break-even cuenta cada slot y cada reserva una vez dentro de la ventana). Depende de cómo PlayerReservasPage datea los slots post-medianoche (inferido de auditorías previas, no re-leído). Sugerencia: atribuir la reserva post-medianoche al día de la jornada, o el disp al día calendario.
- 🔵 [Seguro] porEncimaDelEquilibrio null (contrib<=0 con costos cargados) → DireccionPage:446-450 muestra "Te faltan 0 turnos para dejar de perder" (Math.abs(null)=0) junto a break-even "—". Mensaje contradictorio/confuso, sin crash. El badge (:433) y Termometro (:51) sí guardan null.
- 🔵 [Probable] yield: los 3 segmentos pueden sumar 101 en un borde de redondeo (cada uno Math.round independiente, fugaDescuento clampeado a >=0). Cosmético en la barra.
- 🔵 [Seguro] PATCH /costos actúa como PUT (costos.js:70-83 usa parseCosto que rellena defaults): un update parcial pisa sector/mesesPago/moneda a sus defaults. Hoy no disparado destructivamente (DireccionPage solo toca los costos genéricos; CostosPanel manda body completo con sector+mesesPago). Latente.
- 🔵 [Suposición] contribucion sectores: desdeDate=new Date(`${desde}T00:00:00`) (finanzas.js:233) se interpreta en TZ del server (UTC en Railway) y se compara con pagadoAt (UTC) → borde de la ventana de 30d desalineado ~3h respecto al corte ARG. Ínfimo.

QUÉ NO PUDE VERIFICAR (límites honestos): no corrí el backend ni pegué con curl (cierre por trazado); no confirmé que 'direccion' esté en la matriz de planes/feature-gating (asumí que requireFeature lo resuelve); el mismatch del heatmap cross-midnight depende del dateo de slots post-medianoche en PlayerReservasPage, que no re-leí en esta pasada (lo tomo de auditorías previas que dicen "los slots post-medianoche caen en fecha del día siguiente"); no reproduje ocupación >100% con datos vivos (es por trazado: requiere reservas fuera del horario configurado).

## 2026-06-28 · REDISEÑO CheckoutTurno "comanda" (persistencia instantánea + cobro rápido por persona) + GET /reservas/:id/cuenta — solo lectura · Veredicto: APTO con observaciones (ningún bloqueante de plata; 1 🟠 de doble-cobro del TURNO por convivencia de las dos vías)

Archivos leídos: frontend features/pagos/CheckoutTurno.jsx (completo), backend routes/reservas.js (cobrar :968-1048, GET /cuenta :1053-1070, POST /cuenta :1077-1176), routes/cargos.js (completo: PATCH estado :266, DELETE :245), lib/stock.js (moverStock :4-24).

SÓLIDO (verificado):
- Anti-sobrecobro del turno por valor: guard re-leído DENTRO de la TX Serializable (reservas.js :1131-1137): yaCubierto = (pagado?precio:0) + Σ cargos tipo reserva; si yaCubierto+nuevoTurno > precio → 400. Cubre auto-reparto con redondeo (repartir() en front suma exacta :14-19) y edición a mano. Dos /cuenta concurrentes no superan juntos el precio (re-lee en la TX).
- Stock exacto: descontarStock filtra productoId falsy (stock.js :5) → compartido y "Otro" (productoId null) NO descuentan; consumo normal con productoId descuenta 1 sola vez por su cantidad. persistirCompartido manda N pagos con productoId:null → cero descuentos. Correcto.
- Casual a cuenta bloqueado en los 4 caminos: front persistirConsumo casual→metodoDefault cobrado (:204); resolverPersona casual+cuenta→error (:238); submit simple/split valida (:266,:278); y backend rechaza igual (POST /cuenta :1105) como defensa dura. Sin grieta.
- Idempotencia básica por guard saving: persistirConsumo/persistirCompartido/resolverPersona arrancan con `if (saving) return` + setSaving(true)/finally (:197,:217,:236). El botón "Cobrar $X" y "Confirmar" llevan disabled={saving}. Un doble-click queda absorbido por saving.
- Multi-tenant: GET/POST /cuenta validan reserva.clubId === req.user.clubId (:1059,:1089) y jugadores referenciados con findMany por clubId (:1115). clubId siempre de req.user. Sin fuga.
- Cross-midnight: el cobro NO hace aritmética de horaFin; conceptoTurno usa horaInicio crudo (:1124) y el guard opera sobre montos, no horas. Un turno 22:30→00:00 cobra igual. Sin hermano del bug cross-midnight acá.
- Reapertura/reconstrucción: idsResueltos = jugadores con cargo tipo reserva (:87) se excluyen del split; jugador con consumos a cuenta SIN turno resuelto reaparece (:88). Coherente con la intención.

OBSERVACIONES:
- 🟠 [Probable] DOBLE-COBRO del turno por convivencia de "cobro rápido por persona" (resolverPersona) + "Confirmar global" (submit) en modo split. resolverPersona crea la porción del turno de esa persona vía POST /cuenta (:245) y la SACA del array personas (:255). PERO el submit global (:280-285) mapea TODAS las `personas` con subtotalPersona(p)>0 y vuelve a mandar `turnoMonto:p.turnoMonto`. Caso real: el admin resuelve a Juan con "Cobrar" (queda registrado, sale del split), luego sigue cargando a otros y al final toca "Confirmar $X". Si una persona NO fue resuelta por la vía rápida, su turnoMonto se manda en submit — correcto. El riesgo aparece si el front re-incluye a alguien ya resuelto: hoy resolverPersona lo quita del array, así que submit no lo re-cobra. MITIGANTE FUERTE en el flujo normal. Riesgo residual: el guard backend anti-sobrecobro (:1137) es la última línea — si por auto-split la suma de turnoMonto de los que QUEDAN + lo ya cubierto > precio, el submit entero aborta con 400 (no cobra de más, pero tampoco cobra: el admin ve "El monto del turno supera el precio" y debe re-dividir). O sea: el sistema NO pierde plata ni cobra de más (guard duro), pero la UX puede frustrar y, en el peor caso de edición manual + reapertura, confunde. No es 🔴 porque el backend NO permite que la suma supere el precio. Sugerencia: en submit split, excluir turnoMonto de personas cuya porción ya esté en `cuenta` (tipo reserva mismo jugador), o recalcular turnoMonto contra saldoTurno vivo antes de enviar.
- 🟡 [Seguro] resolverPersona NO es atómico: hace POST /cuenta (turno) y LUEGO N PATCH /cargos/:id/estado secuenciales (:245-252), todo bajo un único `saving` pero en requests separados. Si el navegador/red corta entre el POST y los PATCH, la porción del turno queda cobrada pero los consumos quedan a cuenta (pendientes). No es doble-cobro ni pérdida de plata (queda deuda, recuperable), pero deja un estado parcial. refrescarCuenta() al final lo refleja bien. Aceptable; el cobro real del turno (lo más caro) va primero.
- 🟡 [Seguro] Sobrecobro evitado por backend pero el front puede MOSTRAR un total inconsistente tras editar a mano (autoSplit=false): si el admin escribe turnoMonto a dedo que sumados superan saldoTurno, submit valida `turnoAsignado > saldoTurno` (:276) y corta antes; resolverPersona individual NO valida esa suma en front pero el guard backend (:1137) la corta. Sin pérdida. Cosmético/UX.
- 🔵 [Seguro] persistirConsumo de un casual en modo simple/split lo cobra al instante con metodoDefault (:204) sin dejar elegir método antes de persistir — coherente con "casual paga contado", pero el método queda fijado al default del club (luego editable desde el ticket con TicketLinea). Decisión de UX, no bug.

QUÉ NO PUDE VERIFICAR (límites honestos): no corrí backend ni reproduje con curl la convivencia resolverPersona+submit ni la carrera del 🟡 no-atómico; no leí lib/productos.js snapshotProductos en esta pasada (asumí shape por uso, ya leído en auditoría 2026-06-28); no reproduje el doble-click ultrarrápido real contra `saving` (es trazado de estado React); no verifiqué el render exacto del onDone/cierre del modal en ReservasPage (fuera del archivo auditado).

Veredicto: APTO para cobro real del club. El núcleo de plata está blindado: el guard anti-sobrecobro re-leído en la TX Serializable garantiza que la suma de porciones del turno NUNCA supere el precio (ni por las dos vías, ni por redondeo, ni por edición manual), el stock no se descuenta de más, casual-a-cuenta está rechazado en front y backend, tenant aislado. El 🟠 de convivencia de las dos vías está contenido por ese guard (a lo sumo frustra con un 400, no pierde ni duplica plata). Recomiendo el fix del submit split (excluir turnoMonto ya persistido) como hardening pre-escala, no bloquea la demo/uso.

## 2026-06-28 · COBRAR TURNO FIJO desde la grilla admin (endpoint POST /turnos-fijos/:id/materializar + abrirCheckout en ReservasPage) — solo lectura · Veredicto: APTO con 2 observaciones 🟡 (ningún bloqueante de plata/doble-cobro/doble-booking)

Archivos leídos: backend routes/turnos-fijos.js (completo, foco endpoint materializar :514-554), frontend ReservasPage.jsx (venceCobro :86-95, mapBackendReserva :3101-3149, turnoFijoVirtual+filtro exclusión :3157-3187, abrirCheckout :3265-3288, botón Cobrar turno en DetalleReserva :1507-1514), features/pagos/CheckoutTurno.jsx (completo, submit→POST /reservas/:id/cuenta :194), utils/timeUtils.js (overlaps :58 cross-midnight aware), schema.prisma (Reserva :187-217).

SÓLIDO (verificado):
- Idempotencia anti doble-materialización/doble-cobro: materializar corre bajo runSerializable; findFirst de Reserva existente (clubId,canchaId,fecha,jugadorId,esTurnoFijo,estado≠cancelada) + create dentro de la misma TX Serializable (turnos-fijos.js :531-543). Dos requests concurrentes (dos admins / doble-submit): el segundo aborta por serialización, reintenta, ve la fila commiteada y la devuelve → NUNCA crea dos Reservas para el mismo día. El cobro real va por POST /reservas/:id/cuenta (ya auditado 2026-06-28: anti-sobrecobro re-leído DENTRO de la TX). El materializar NO marca pagado (pagado:false :541) → la acción de "preparar cobro" está desacoplada del cobro. Correcto.
- Multi-tenant: clubId siempre de req.user.clubId (:521); el findFirst del turno filtra por {id, clubId} (:524) → un admin no puede materializar un TF de otro club (404). Sin fuga.
- Cross-midnight: el endpoint copia horaInicio/horaFin del TF tal cual (:539); un TF 22:30→00:00 sólo puede existir si pasó la validación duracion===90 (turnos-fijos.js :154, duracionMin cross-midnight aware), así que horaFin sólo puede ser '00:00' (nunca 00:30/01:00). venceCobro (ReservasPage :86-95) usa el patrón correcto `if (finMin <= toMin(horaInicio)) finMin += 1440` (NO el antipatrón `==='00:00'?1440`) → un TF 22:30→00:00 da finMin=1440, no vence durante su jornada. La Reserva materializada hereda ese horaFin y el cobro/caja lo calcula igual. Sin deuda/cobro fantasma nocturno.
- Estado 'debe' vs 'pendiente' coherente: el TF virtual usa `venceCobro(fecha,t.inicio,t.fin)` (:3181) — el MISMO helper y misma firma que mapBackendReserva para reservas online (:3121). Consistente.
- Permiso 'ventas' coherente: el cobro (POST /reservas/:id/cuenta) usa requirePermiso('ventas') (auditoría 2026-06-28); materializar también (:518). Un admin sin 'ventas' no ve el botón porque DetalleReserva sólo lo pinta si `onCheckout` está presente, y el flujo de cobro es exclusivo del rol con ventas. (Nota: el botón "Cobrar turno" se muestra por estado/tipo, no gatea por permiso en el front — pero el backend rechaza con 403 si no tiene 'ventas', así que no hay acción real sin permiso. Defense en backend OK.)
- Consistencia de grilla post-cobro: el TF virtual se excluye en cuanto existe una Reserva no-cancelada que solapa su slot (filtro :3166-3170 con overlaps cross-midnight). abrirCheckout hace fetchReservasBackend(fecha) tras materializar (:3271) → la Reserva real entra a reservasBackend y el virtual desaparece del próximo render. La Reserva materializada tiene tipo:'fijo' (violeta, mismo color) → visualmente continuo.

OBSERVACIONES (no bloqueantes):
- 🟡 [Seguro] DOBLE-SUBMIT del botón "Cobrar turno" sin guard de UI durante el await del materializar. El botón (ReservasPage :1507-1513) NO tiene disabled durante la llamada, y abrirCheckout (:3265) no setea ningún flag `materializando`. MITIGANTE FUERTE: la primera línea de abrirCheckout es `setSeleccion(null)` (:3266), que desmonta el modal DetalleReserva donde vive el botón → en la práctica el botón deja de existir tras el primer click, cerrando la ventana de doble-click. Y aunque dos POST /materializar entraran (p.ej. doble-click ultrarrápido antes del re-render), el endpoint es idempotente bajo Serializable → devuelven la MISMA Reserva, sin doble cobro ni doble reserva. Resultado posible bajo doble-disparo: dos modales CheckoutTurno apilados sobre el mismo _backendId (cosmético, no plata). Riesgo frente al dueño: ~nulo en plata, a lo sumo un modal duplicado raro. Sugerencia (hardening): setear un estado `preparandoCobro` y disabled en el botón, o guard `if (preparando) return` en abrirCheckout. Bajo prioridad por el doble mitigante.
- 🟡 [Probable] El endpoint materializar NO re-valida conflicto/solape con OTRA reserva real existente en el slot — confía en que el slot es del TF. Análisis: el botón sólo aparece sobre el TF VIRTUAL, y el virtual ya se excluye de la grilla si existe una Reserva no-cancelada que solapa su slot (:3166-3170). O sea: si hubiera una reserva real pisando el slot, el admin NO vería el TF virtual y no llegaría a materializar. Además, el findFirst idempotente acota a esTurnoFijo:true del MISMO jugador, así que no "secuestra" una reserva online ajena. Riesgo residual: si por una carrera el slot fue ocupado por una reserva online JUSTO entre que la grilla se renderizó y el admin tocó "Cobrar", el materializar crearía igual una segunda Reserva (esTurnoFijo) solapada con la online → doble-booking lógico de ese slot (ambas confirmadas). Es una ventana TOCTOU estrecha y poco frecuente (requiere que alguien reserve ese mismo slot del TF en segundos, lo cual el propio TF normalmente bloquea aguas arriba). No es doble-COBRO (son reservas distintas). Sugerencia: agregar dentro de la TX de materializar un chequeo de conflicto con conflictoEnDia / overlaps sobre reservas no-fijas del slot antes de crear, abortando 409 si el slot ya está tomado por otra reserva. Defense-in-depth, no urgente.

QUÉ NO PUDE VERIFICAR (límites honestos): no corrí el backend ni reproduje con curl (cierre por trazado); no reproduje la carrera del 🟡 TOCTOU con dos requests reales; asumí (por auditoría previa 2026-06-28) que POST /reservas/:id/cuenta mantiene el guard anti-sobrecobro re-leído en TX — no lo re-leí en esta pasada; no verifiqué visualmente el render del modal apilado del 🟡 doble-submit (es inferencia del flujo de estado React).

Veredicto: APTO para que el dueño lo use. El núcleo de plata está bien: materializar es idempotente bajo Serializable (no duplica reserva ni cobra dos veces), el cobro reusa el camino ya blindado /cuenta, tenant por JWT, cross-midnight correcto con el helper bueno, estado 'debe'/'pendiente' consistente con reservas online. Las 2 observaciones 🟡 son hardening de concurrencia con mitigantes fuertes en el flujo real de UI; ninguna rompe plata en el uso normal.

## 2026-06-28 · BLOQUE PAGOS / COBRO completo (Ventas+Mesas, Stock, Cobranzas, Gastos, Caja, Checkout de turno) — diagnóstico solo-lectura pre-cliente · Veredicto: APTO con observaciones (ningún bloqueante de plata)

Primera auditoría del bloque financiero (no estaba en el registro). Archivos leídos completos: backend routes/cargos.js, productos.js, gastos.js, caja.js, comandas.js, reservas.js (endpoints de cobro :940-1171), lib/stock.js, lib/deudas.js, lib/tiempo.js, app.js (montaje de routers :64-88); frontend features/pagos/CheckoutTurno.jsx, VentasTab.jsx, GastosTab.jsx, CajaTab.jsx, StockTab.jsx, pages/PagosPage.jsx (Cobranzas + Cuenta de jugador).

SÓLIDO:
- Atomicidad del dinero: los 2 checkouts del turno (POST /reservas/:id/cobrar :968 y /:id/cuenta :1055) corren bajo runSerializable con guard anti-sobrecobro re-leído DENTRO de la TX (:1109-1115: yaCubierto + nuevoTurno > precio → 400). Idempotentes (re-leen cargo pendiente del turno → doble-submit no marca pagado dos veces ni duplica deuda).
- cobrar-cuenta (cargos.js :210) y comandas/cerrar (:147): $transaction array sin Serializable, PERO idempotentes por el where acotado (updateMany estado:'pendiente' / pagado:false) → un doble-submit no re-cobra ni duplica. Correcto para saldar deudas YA existentes (no crean montos nuevos).
- Stock (lib/stock.js): descuento/reposición siempre dentro de la misma TX que el cargo (venta, comanda items, /cuenta consumos). reponerStock en anular/eliminar/quitar ítem. Solo afecta productos controlaStock:true (filtrado en moverStock). No vi camino que descuente dos veces ni deje negativo de forma no intencional (increment atómico; stock puede ir a 0 o negativo solo por ventas concurrentes del mismo producto sin guard de stock>=0, ver observación).
- Multi-tenant: TODOS los queries de plata filtran por req.user.clubId; jugadorId/productoId se re-validan contra el club antes de escribir (cargos POST :180, venta :161, cobrar-cuenta :221, /cuenta :1093). Sin fuga de tenant.
- Permisos: a nivel montaje en app.js — productos/comandas/categorias = requireFeature('finanzas')+requirePermiso('ventas'); gastos/caja = +requirePermiso('caja'); cargos.js los aplica por-endpoint (todos requirePermiso('ventas')); reservas cobro endpoints requirePermiso('ventas'). NINGÚN endpoint de plata quedó sin guard. (Descarté mi sospecha inicial: productos/gastos/caja parecían sin guard al leer el router, pero están en el app.use.)
- Caja (caja.js): usa rangoDiaArg (UTC-3, cross-midnight correcto), cuenta SOLO pagado (reserva.pagado / cargo.estado='pagado' / gasto.pagado) por pagadoAt en el rango. Ingresos=reservas+cargos, egresos=gastos. Los cargos de comanda entran a caja al cerrarse (pasan a pagado con pagadoAt) — correcto, son ingreso real. Cobranzas/resumen (cargos.js) excluyen comandaId:null para no mezclar deudas de mesa con deudas de jugador — coherente.
- deudas.js: turnosImpagosDeuda usa finEnMin (cross-midnight aware, helper único). NO repite el antipatrón `horaFin==='00:00'?1440`. Sin deuda fantasma nocturna.
- Casos límite cubiertos: casual/mostrador a cuenta RECHAZADO en los 4 caminos (cargos POST :169, venta :141, /cobrar :980, /cuenta :1083); cobro parcial del turno soportado (/cuenta repetible con guard de saldo); anular cobro (vuelve a pendiente, sale de caja); montos <=0 rechazados (Math.round + >0 en todos los POST); producto sin controlaStock → moverStock lo ignora sin error.
- Doble-submit frontend: todos los submit con setSaving+disabled={saving} y éxito tras la API (CheckoutTurno :192-199, VentasTab cerrar :169-179 muestra éxito tras await, GastosTab, StockTab, PagosPage cobrar/anular/eliminar/cambiarMetodo). Commit-timing correcto (pantalla de éxito de mesa se setea recién tras /cerrar). Sin localStorage de negocio.

OBSERVACIONES (no bloqueantes):
- 🟡 [Seguro] Venta de stock puede dejar stock NEGATIVO bajo concurrencia o sobre-venta. lib/stock.js moverStock (:12) hace `increment: signo*cantidad` sin chequear stock>=cantidad antes. Dos ventas concurrentes del mismo producto, o vender más de lo que hay, llevan stock a negativo (el front muestra unidades pero no bloquea vender sin stock). No rompe plata (el cargo y el cobro son correctos), pero el inventario miente. Para un dueño de club es un "−3 Gatorade" raro. Sugerencia: validar stock disponible en la TX o aceptar negativo como decisión explícita (sobreventa permitida).
- 🟡 [Seguro] DELETE /cargos/:id (cargos.js :245) borra un cargo SIN chequear estado: si está pagado, lo saca de la caja (la caja se recalcula on-the-fly desde cargos pagados). Hoy NO es alcanzable desde la UI (PagosPage solo ofrece "Eliminar" sobre estado pendiente :890-905; sobre pagado ofrece Anular→pendiente). Riesgo latente: un request directo a DELETE de un cargo pagado altera la caja de un período ya cerrado sin dejar rastro. Sugerencia: rechazar DELETE si estado='pagado' (forzar Anular primero) como defense-in-depth.
- 🔵 [Seguro] /reservas/:id/cuenta (:1143) lee reserva.cobroOmitido FUERA de la TX para decidir el update de neutralización. Idempotente igual (update a true es inocuo si ya estaba), pero conviene re-leer dentro de la TX por consistencia con el resto del guard. Cosmético.
- 🔵 [Suposición] PATCH /cargos/:id/estado permite reabrir (pendiente) o condonar sin validar transición previa; no vi forma de que rompa caja (recálculo on-the-fly), pero no tracé todas las combinaciones condonado↔pagado↔pendiente con cargos de comanda. Bajo.

QUÉ FALTA (features, priorizado): (1) integración de cobro online / MercadoPago — hoy todo es registro manual de método (declarado Fase 2 en memorias, no es bug); (2) OCR de factura en Gastos/Compra está como placeholder ("Próximamente", StockTab :392) — UI lista, sin backend; (3) recibo/comprobante fiscal real (hoy imprime ticket no fiscal vía comprobantes.js); (4) guard de stock negativo (ver 🟡).

Veredicto: APTO para mostrar a un dueño de club. El núcleo de plata (checkouts, caja, cobranzas, anti-sobrecobro, anti-doble-cobro, tenant, permisos, cross-midnight) está bien construido. Las 2 observaciones 🟡 son hardening, no rompen plata en el uso normal por la UI.

Sin verificar (límites honestos): no corrí el backend ni reproduje con curl (cierre por trazado); no leí lib/productos.js (snapshotProductos) ni lib/metodosPago.js (normalizarMetodo) en detalle — asumí shape por uso; no tracé el flujo de inscripción a torneo con su unique anti-doble-cobro (fuera del alcance de esta pasada, vive en torneos.js); no audité comprobantes.js (impresión/CSV) más allá de su firma; el riesgo de stock negativo es por trazado, no reproducido con dos requests concurrentes reales.

## 2026-06-25 · MATRIZ DE CONCURRENCIA del módulo reservas — cobertura vs suite scripts/concurrencia.mjs (12 verdes) · Veredicto: suite INCOMPLETA (faltan escenarios, 2 caminos sin Serializable)

No es una auditoría de "apto/no apto" sino un mapeo de qué escenarios de carrera NO cubre la suite actual. Archivos leídos completos: reservas.js, turnos-fijos.js, lib/conflictos.js, lib/serializable.js, lib/convocatorias.js, routes/solicitudes.js, routes/convocatorias.js (voy/baja), scripts/concurrencia.mjs.

HALLAZGOS DE CÓDIGO (caminos que la suite no ejercita y el código podría no cubrir):
- 🔴 [Seguro] `PATCH /:id/ausencia/:fecha` (turnos-fijos.js:458, ADMIN libera/crea ausencia) NO usa runSerializable: hace `turnoFijo.update push diasAusentes` + `reserva.update cancelada` con prisma plano, sin TX, sin re-lectura serializable. Es el ÚNICO camino que abre un hueco en un TF sin aislamiento. Carrera: admin libera la fecha (push diasAusentes) ⟂ jugador/otro admin reserva ese hueco que conflictoEnFecha recién ve libre cuando el push commitea → ventana TOCTOU. No produce doble-booking de dos reservas (eso sí lo protege el create de reserva), pero sí lecturas sucias del estado del TF. Contraste: la ausencia del JUGADOR (turnos-fijos.js:363) SÍ está bajo runSerializable. Asimetría a cerrar.
- 🟠 [Probable] Reactivar ausencia (quitar fecha de diasAusentes) NO existe como endpoint → no auditable. Hoy una fecha liberada no se puede "re-ocupar" salvo recrear el TF. Si alguien agrega ese path, debe ir bajo Serializable + re-chequear que no haya reserva ya creada en el hueco (invariante: no se puede reactivar si el hueco ya fue tomado). Documentar como gap futuro.
- 🟡 [Seguro] `armarFixtureConvocatoria` se dispara FUERA de la TX del /voy (convocatorias.js:294-298) tras contar voyCount con prisma plano. Dos /voy concurrentes que completan el cupo pueden disparar armarFixture dos veces (idempotencia depende de armarFixture, no del Serializable). No es doble-booking de cancha (las canchas ya están reservadas desde organizarConvocatoria), pero puede duplicar fixture/estado. Verificar idempotencia de armarFixtureConvocatoria (no leí su cuerpo completo en esta pasada).

ESCENARIOS FALTANTES EN LA SUITE (entregados a Luca en el mensaje, agrupados A–I): ausencia↔reserva del hueco (jugador y admin), auto-confirm ON/OFF no cambia invariante de slot (pendiente bloquea igual que confirmada — verificado en conflictos.js ACTIVOS_RESERVA incluye 'pendiente'), confirmar TF pendiente ⟂ reserva en próxima ocurrencia, convocatoria atómica N canchas (todo-o-nada) + 2 convocatorias por mismas canchas, /voy de solicitudes (sí usa Serializable, solicitudes.js:88), clase mismo profesor 2 canchas simultáneo (chequeo profesorId está DENTRO de la TX:456 → cubierto, falta test), doble cancelación + cargo (DELETE reservas.js:1195 NO está bajo TX → posible doble cargo de cancelación), borde medianoche/cambio de día.

Sin verificar (límites): no corrí la suite ni agregué escenarios (el pedido fue diseñar la matriz, no codear); no leí el cuerpo de armarFixureConvocatoria ni descontarStock; el riesgo del DELETE doble-cancelación es por trazado (no hay guard de estado dentro de TX), no reproducido con curl.



## 2026-06-24 · ADVERSARIAL cross-midnight (00:30/01:00) en TODO reservas — caza de hermanos del bug de display · Veredicto: NO APTO (2 hallazgos de PLATA + display)

Contexto: ya se arreglaron 2 display (PlayerMisReservasPage, PlayerDashboardPage) con el patrón correcto `toMin(horaFin) <= toMin(horaInicio)`. Busqué todos los hermanos del guard angosto `horaFin === '00:00' ? 1440 : toMin(horaFin)`.

CAUSA RAÍZ (clave): `getCierreOpciones` (QuienesSomosPage.jsx:708-724) ofrece cierres = apertura + N×90 hasta ~03:00 (limit 1440+180). Eso produce cierres post-medianoche con minutos NO-00:00: 00:30, 01:00, 01:30, 02:00. Los motores de slots (generateFranjas ReservasPage.jsx:127 / timeUtils.js:183 / generarSlots PlayerReservasPage.jsx:66, todos cross-midnight aware con `ciMin <= apMin → +1440`) generan franjas de 90' con `horaFin` tipo '00:30','01:00','02:00'. RESERVAS EVENTUALES y CLASES backend NO validan duración=90 (reservas.js POST jugador :327 y POST profesor confían en el horaFin del front) → esas filas SÍ llegan con horaFin post-medianoche ≠ '00:00'. El guard plano `=== '00:00' ? 1440` invierte el rango para ellas (toMin('00:30')=30 < inicio).

Hallazgos (priorizados):
- 🔴 [Seguro] deudas.js:25 — `const finMin = r.horaFin === '00:00' ? 1440 : aMin(r.horaFin)`. Reserva eventual de HOY 23:30→01:00 da finMin=60; `60+60 <= ahoraMin` es verdadero casi todo el día → el turno se marca DEUDA / cobro pendiente ANTES de jugarse. Plata mal calculada de cara al dueño (turnosImpagosDeuda alimenta cobros pendientes del dashboard y panel de deudas). Aplica a reservas eventuales/clases, NO a TF (TF valida 90 con guard plano → no puede tener fin ≠00:00).
- 🟠 [Seguro] ReservasPage.jsx:91 (`venceCobro`) y :1360 (`finParaCorte = fin==='00:00'?'24:00':fin`, `yaTermino`) — misma inversión para reserva eventual 23:30→01:00: `venceCobro` da true desde temprano → la grilla la trata como "vencida para cobro"/"ya terminó" durante su propia jornada, habilitando/bloqueando acciones antes de tiempo. Riesgo operativo de cobro y de bloquear cancelación de un turno aún futuro.
- 🟠 [Seguro] clubs.js:138 (`enCurso` dentro de GET /clubs/me/dashboard, ocupadasAhora :141 + agenda) y AdminDashboardPage.jsx:233 (`enCurso` de la agenda) — para reserva/clase 23:30→01:00, `f = toMin('01:00')=60`, condición `i <= ahora && ahora < f` nunca marca en curso pasada la medianoche y mal el "% canchas en uso ahora" / badge "En curso". Display del pulso en tiempo real incorrecto en horario nocturno. (El backend pasa horaFin crudo al front, AdminDashboardPage repite el mismo guard → doble exposición.)
- 🟡 [Seguro] ClasesProfesorAdminPage.jsx:38 (`duracionHs`: `toMin(fin==='00:00'?'24:00':fin) - toMin(inicio)`) — clase 23:30→01:00 da diff = 60-1410 = negativo → `return 0`: la métrica de horas/duración de la clase sale en 0. Solo afecta métricas semanales de clases, no plata directa.
- 🔵 [Seguro] PlayerTurnosFijosPage.jsx:62 (`turnoYaTerminoHoy`, guard plano) — quedó con el patrón viejo, PERO para TF es inofensivo: un TF no puede tener horaFin ≠ '00:00' (la validación duracion!==90 en turnos-fijos.js:154-156 lo rechaza). Display correcto hoy. Conviene unificarlo igual por consistencia/futuro.

CONFIRMADO BIEN (el `=== '00:00' ? 1440` es CIERRE de club o aplica solo a TF, no rompe):
- PlayerReservasPage.jsx:55 (snapCierreToSlots) y :74 (generarSlots) → operan sobre el CIERRE del club, donde 00:00 SÍ es 1440, y además adelante hacen `ci < ap → +1440` (cross-midnight real). OK.
- ReservasPage.jsx:131 / timeUtils.js:187 / clubs.js:21,138(franjasDia) → CIERRE de club, con `ci <= ap → +1440` posterior. El generador es correcto para cierres con minutos; el bug NO está en generar slots sino en los CONSUMIDORES que reciben horaFin de una fila ya creada.
- insight.js:32,40 y QuienesSomosPage.jsx:730-733 → CIERRE de club. OK.
- turnos-fijos.js (:154,:174,:180,:274,:297,:312) → guard plano sobre horaFin de TF, pero la validación duracion===90 garantiza que el único cross-midnight posible de un TF es '...→00:00'. Sin grieta (ya auditado 2026-06-20, sigue válido).
- reservas.js overlaps()/rangoMin() (:19-34) → cross-midnight aware por diseño (`f <= i → +1440`). El anti doble-booking de reservas eventuales NO se rompe con 00:30/01:00: el solape se calcula correcto. (El bug es solo de cobro/deuda/display, NO de doble-booking.)
- PlayerMisReservasPage.jsx:173 y PlayerDashboardPage.jsx:370 → ya corregidos con `toMin(horaFin) <= toMin(horaInicio)`. Patrón correcto. ProfesorAgendaPage.jsx:48 también correcto (`end <= cur → +1440`).

Zona horaria: deudas.js usa hoyArgStr()/ahoraArgHHMM() (ARG UTC-3) — correcto para "ya jugó/vencido". Los display de Player (PlayerMisReservas/Dashboard) usan `new Date()` local del navegador: si el dueño/jugador está en ARG no hay desfase; si abre desde otra TZ podría desalinear el corte "Finalizado" (BAJO, fuera de alcance hoy). reservas.js POST valida "ya comenzó" con UTC-3 y comparación lexicográfica horaInicio<=horaAhora — correcta para el día de la fila (los slots post-medianoche caen en fecha del día siguiente, no en hoyArg). Comparaciones de fecha string 'YYYY-MM-DD' (deudas.js:26 `r.fecha < hoyStr`) son lexicográficas pero válidas por formato ISO fijo. Sin hallazgo ahí.

Sin verificar (límites honestos): no corrí backend ni reproduje con curl (cierre por trazado); no confirmé dinámicamente que un admin real haya configurado un cierre tipo 01:30 en datos vivos — el riesgo es REAL pero depende de que el club use horario nocturno cross-midnight (clubes de pádel que cierran 01:00/02:00 es común). `permitirCruceMedianoche` (clubStore.js:63) está en false y NO se lee en ninguna lógica (es flag muerto) → no gatea nada: los slots cross-midnight se generan igual desde getCierreOpciones. No audité si existe alguna validación de duración en el POST de clase admin que recorte horaFin.

Veredicto: NO APTO mientras un club pueda cerrar pasada la medianoche con minutos. Los hallazgos 🔴/🟠 tocan PLATA (deuda/cobro fantasma) y el pulso en vivo. Fix sugerido (no implementado): reemplazar todos los `x.horaFin === '00:00' ? 1440 : toMin(x.horaFin)` que operan sobre horaFin de una RESERVA/CLASE por el patrón cross-midnight real (`fin <= inicio → fin+1440`), o centralizar en un helper `finMinDeReserva(horaInicio, horaFin)`. Los que operan sobre CIERRE de club están bien y no se tocan.

## 2026-06-24 · MÓDULO RESERVAS completo (eventuales + turnos fijos + convocatorias) — foco reglas de negocio · Veredicto: APTO con 2 observaciones (ninguna bloqueante de doble-booking/plata)

Auditoría crítica pre-cliente. Archivos leídos completos: `backend/routes/reservas.js`, `backend/routes/turnos-fijos.js`, `backend/routes/convocatorias.js`, `backend/lib/serializable.js`, `backend/lib/deudas.js`, `backend/lib/convocatorias.js`, `prisma/schema.prisma` (Reserva/TurnoFijo/Cargo), `frontend/PlayerReservasPage.jsx` (generarSlots :47-186 + handleConfirmar :462), `frontend/PlayerMisReservasPage.jsx` (cancelación).

ANTI DOBLE-BOOKING — BLINDADO. Los 8 caminos de creación/confirmación corren bajo runSerializable: reservas POST jugador (:327), POST profesor (:434), PATCH profesor (:545), POST admin clase-profesor (:605), POST admin (:689), PATCH confirmar (:834); turnos-fijos POST jugador (:171), PATCH confirmar (:290), POST ausencia (:413); convocatorias organizarConvocatoria (:27) y POST /voy + /baja. serializable.js correcto (Serializable + retry P2034/40001). Cross-midnight cubierto: reservas.js/convocatorias.js usan overlaps()/rangoMin() (fin<=inicio → +1440, espacio 48h); turnos-fijos.js usa toMin plano con guard `=== '00:00' ? 1440` en TODOS los puntos de comparación. deudas.js mismo guard (:25).

Hallazgos:
- 🟠 [Seguro] Convocatorias permiten reservar canchas en FECHA/HORA YA PASADA. `lib/convocatorias.js organizarConvocatoria` (:22-52) NO valida que fecha/horaInicio no hayan pasado, a diferencia de TODOS los demás POST de reservas que tienen el guard ahoraArg "El turno ya comenzó" (reservas.js :307,:416,:676). Los endpoints POST /convocatorias (:34) y POST /convocatorias/mias (:156) llaman directo sin re-validar (grep en convocatorias.js: cero matches de hoyArg/pasado). El front filtra horas pasadas en slots-libres, pero el backend confía en el front → un request directo o desfase de reloj crea reservas confirmadas sobre slots muertos, ocupando canchas en el pasado. Riesgo frente al dueño: bloqueos fantasma en la grilla. Sugerencia: replicar el guard ahoraArg en organizarConvocatoria.
- 🟡 [Seguro] Convocatorias IGNORAN el toggle autoConfirmaReservas: la reserva nace siempre `estado:'confirmada'` hardcodeado (convocatorias.js :47), sin pasar por clubAutoConfirma. Si el dueño apagó la auto-confirmación, una convocatoria igual ocupa las canchas al instante sin aprobación admin. Hoy es coherente con que el organizador "ya reservó", pero es una inconsistencia con el resto del módulo que conviene documentar como decisión explícita. Sin daño de doble-booking (el solape sí se valida).
- 🔵 [Seguro] Branch muerto latente en reservas.js POST `/` con esTurnoFijo:true (ya reportado 2026-06-20): ningún caller del front lo dispara. Cosmético.

Verificado OK: slots 1.5h (front genera en pasos de 90' :80; TF valida duracion===90 :156; convocatorias sumar90 :15,:23 — nunca +1h); estados correctos por modelo (reserva eventual nace 'confirmada'/'pendiente' según toggle :323; TF 'confirmado'/'pendiente' :166; schema defaults Reserva.pagado=false :197); auto-confirmación respetada en reservas+TF vía clubAutoConfirma; cancelación+cargo: ventana horaria con Date.UTC(...,h+3) ARG→UTC correcta (reservas.js :1275, turnos-fijos.js :431), cargo solo si precio>0 + fuera de plazo, re-leído dentro de TX en ausencia; bloqueo por deuda consistente (DELETE TF jugador :363→409, PATCH inactivo admin :252→409, baja jugador en otro módulo); multi-tenant por req.user.clubId en TODOS los queries (body.clubId ignorado en reservas POST :295; convocatorias siempre req.user.clubId); doble-submit OK (handleConfirmar guard `|| submitting` + setSubmitting + return en catch :463,:493,:529; PlayerMisReservas `cancelando`+disabled :222,:427); commit-timing OK (pantalla de éxito recién tras la API :552); RN-51 con overlaps cross-midnight (admin :740-751); confirmación de TF re-valida reserva eventual conflictiva en próximas 8 ocurrencias dentro de TX (:307-320); voy/baja de convocatoria bajo Serializable sin sobrecupo; transiciones de estado: cancelar/rechazar no necesitan TX (no crean slot); PATCH /:id/estado confirmar re-verifica solape antes de reactivar (:834).

Sin verificar (límites honestos): no corrí backend ni reproduje concurrencia con curl (cierre por trazado); sigue pendiente el índice único parcial en DB (canchaId+dia/fecha+horaInicio para estados activos) como defense-in-depth — deuda conocida, hoy la única defensa es el Serializable a nivel app, correctamente aplicado; no re-validé dinámicamente la aritmética UTC-3 del cargo; no audité PlayerDashboardPage ni ReservasPage admin (grilla) en esta pasada — la consistencia de los 3 roles la inferí del backend compartido, no del render admin/profesor.


## 2026-06-20 · NOTIFICACIONES del bloque automatización (branch automatizacion-turnos) — trazado punta a punta · Veredicto: APTO para cliente

Foco exclusivo: que ninguna notificación llegue rota/en blanco/mal mapeada en ningún rol. Archivos leídos: `backend/src/routes/reservas.js` (completo), `backend/src/routes/turnos-fijos.js` (completo), `frontend/src/store/notificacionesStore.js` (normBackend), `frontend/src/store/playerNotificationsStore.js` (TITULOS+formatCuerpo), `frontend/src/components/ui/Navbar.jsx` (formatNotif), `frontend/src/pages/ReservasPage.jsx` (panel notif :2106-2475), `frontend/src/pages/PlayerNotificacionesPage.jsx` (TIPO_META+render), `frontend/src/pages/PlayerReservasPage.jsx` (qué endpoint dispara cada notif).

Hallazgos:
- 🔵 [Seguro] Branch latente en `reservas.js` POST `/` con `esTurnoFijo:true` arma `dataNotif` SIN `dia` (reservas.js :367-375) y emite `turno_fijo_confirmado`/`turno_fijo_autoconfirmado`. El render jugador (`formatCuerpo` :28) y admin (`esTurnoFijoAuto` ReservasPage :2450, Navbar :14) usan `dia` → si ese camino corriera, el cuerpo saldría con hueco ("todos los  · ..."). NO es bug activo: ningún caller del frontend postea a /reservas con esTurnoFijo:true (grep `esTurnoFijo:true` → 0 matches; PlayerReservasPage solicita TF por POST /turnos-fijos :470-475, que SÍ manda `dia` vía crearNotifTurnoFijo). Riesgo: latente si alguien reactiva ese branch. Sugerencia: agregar `dia` al dataNotif de ese branch o eliminar el branch muerto.

Verificado OK tipo por tipo (data backend → normBackend/store → render):
- `reserva_autoconfirmada` (admin): backend manda jugador/canchaNombre/fecha/horaInicio/horaFin/precio (reservas.js :367). normBackend mapea todos. Render ReservasPage :2433-2441 (jugador+precio+cancha+inicio/fin+fechaReserva) y Navbar :11-12. Excluido del fallback (:2462). Pasa notifFiltradas. OK.
- `turno_fijo_autoconfirmado` (admin): backend manda dia+jugador+cancha+horas+precio (turnos-fijos.js :209-217). Render :2443-2451 usa DIAS_LABEL[n.dia] y precio; Navbar :13-14 usa n.dia. OK.
- `turno_liberado_auto` (admin): backend manda turnoFijoId+fecha+jugador+canchaNombre+dia+horas (turnos-fijos.js :484-492). Render :2453-2460 usa jugador+cancha+inicio/fin+fechaReserva; Navbar :15-16. Excluido del fallback. OK.
- Refactor ausencia (turnos-fijos.js :405-505): la TX devuelve `{turno, updated, cargoAplicado, horasMinimas}` (:467) y se desestructura bien (:470). Las 3 notifs (cargo_cancelacion :474, turno_liberado_auto :494, ausencia_confirmada :497) se arman FUERA de la TX con todos los campos. `dataNotif` toma jugador/cancha de `updated` (incluye INCLUDE_CANCHA) y dia/horas de `turno`. cargo_cancelacion toma monto=turno.precio y horasMinimas de result. Correcto.
- `ausencia_confirmada` (jugador): TITULOS+formatCuerpo :32 (cancha+fecha+horas). TIPO_META :61. OK.
- `reserva_confirmada` / `turno_fijo_confirmado` (jugador): TITULOS+formatCuerpo :25/:28, TIPO_META :13/:37. El `turno_fijo_confirmado` real (POST /turnos-fijos) trae `dia` → cuerpo completo. OK.
- `cargo_cancelacion` (jugador): formatCuerpo :33 usa fecha+horaInicio+monto; backend manda monto+horasMinimas (reservas.js :1305 y turnos-fijos.js :474). OK.
- `nueva_reserva` / `solicitud_turno_fijo` (flag OFF): Navbar :9-10 + :17 y ReservasPage esSolicitudFijo :2302. nueva_reserva se filtra del panel ReservasPage (notifFiltradas :2107) porque las pendientes se listan aparte; solicitud_turno_fijo pasa y se renderiza. OK.
- Fallback admin ReservasPage (:2462): excluye explícitamente esReservaAuto/esTurnoFijoAuto/esTurnoLiberadoAuto → ninguno cae en fila en blanco. notifFiltradas (:2106) es blacklist de nueva_reserva+torneos; los 3 tipos auto pasan. Navbar default (:29-30) muestra el tipo legible (replace _→espacio), nunca blanco.
- Página jugador PlayerNotificacionesPage: TIPO_META tiene fallback genérico (icono Bell, :121) → aunque falte el meta visual, siempre muestra titulo+cuerpo. Sin fallback vacío.
- Fire-and-forget: TODAS las prisma.notificacion.create llevan .catch(()=>{}) o .catch(console.error) y corren fuera de runSerializable → un fallo de notif no aborta ni revierta el flujo principal (reserva/turno/cargo ya commiteados). Verificado en los 6 caminos del bloque.

Sin verificar (límites honestos): no corrí backend ni validé dinámicamente con curl (cierre por trazado de código); no revisé el endpoint backend /notificaciones (admin/me) que persiste y devuelve las filas (asumí que devuelve `{id,tipo,data,leida,createdAt}` tal como lo consumen normBackend y el store jugador — [Suposición], no leí ese router); no audité tipos de notif fuera del bloque automatización (torneos, stock, clase profesor) salvo donde comparten render.

Veredicto: APTO. Cada tipo de notif del bloque emite los campos que su render consume, en los tres roles. El único hallazgo es un branch muerto de severidad cosmética y latente. Ninguna notificación llega rota o en blanco en los caminos que el frontend realmente dispara hoy.


## 2026-06-20 · ADVERSARIAL doble-booking / plata — branch automatizacion-turnos (foco solapamiento + cargos ausencia) · Veredicto: APTO (sin grieta)

Pasada adversarial deliberada: intenté colar un doble-booking, un cargo duplicado o un estado inconsistente. No encontré ninguno. Archivos releídos: reservas.js (helpers toMin/rangoMin/overlaps :13-34, POST jugador :327, POST admin :688, PATCH confirmar :833), turnos-fijos.js (POST jugador :171, PATCH confirmar :290, POST /:id/ausencia :413, PATCH admin ausencia :518), deudas.js (turnosImpagosDeuda :14), tiempo.js, serializable.js, schema.prisma (pagado defaults).

ÁREA 1 — solapamiento/doble-booking: BLINDADO.
- Todos los caminos de creación/confirmación corren bajo runSerializable (Serializable + reintento P2034/40001, serializable.js correcto). No hay ningún path que escriba reserva/TF fuera de runSerializable. PATCH estado a no-confirmado (cancelar/rechazar/inactivo) no necesita TX (no crea slot).
- Cross-midnight: reservas.js usa overlaps()/rangoMin() (fin<=inicio → +1440, espacio extendido 48h :29-32) — cross-midnight aware por diseño, cubre el 00:00. turnos-fijos.js usa toMinBE/toMinLocal planos PERO con guard explícito `horaFin === '00:00' ? 1440` en TODOS los puntos de comparación de rango (:154,:174,:180,:274,:297,:312) y en la validación de duración 90 (:154). El guard solo cubre el caso exacto '00:00'; cualquier otro fin post-medianoche (ej. 01:00) sería rechazado antes por duracion!==90, así que no hay slot que escape el guard. Sin grieta.
- RN-51 + admin pisando TF: POST /admin (:739-750) re-valida con overlaps() y aborta 409 si ya hay TF pendiente/confirmado solapado; el bloqueo de reserva eventual sobre TF confirmado respeta diasAusentes + desde (:702-715). Admin NO puede pisar un TF confirmado salvo día liberado por ausencia.
- Doble-aprobación concurrente de TF: PATCH confirmar re-verifica TF solapado + reserva/clase conflictiva DENTRO de la TX (:292-320); la segunda transacción aborta y al reintentar ve la fila commiteada → 409.

ÁREA 2 — plata: BLINDADO.
- deudas.js turnosImpagosDeuda: un turno es deuda solo si confirmada + pagado:false + cobroOmitido:false + jugadorId + precio>0 + venció gracia (fecha<hoy, o hoy con finMin+60<=ahora). Guard medianoche en horaFin (:25): un turno que termina 00:00 da finMin=1440, nunca <= ahoraMin → no se marca deuda antes de jugarse. Un turno futuro/no jugado NO aparece como deuda. Correcto.
- POST /:id/ausencia (refactor bajo runSerializable :413): el check `ausenciasPendientes/diasAusentes.includes(fecha)` se RE-LEE dentro de la TX (findUnique :414 → check :418) antes de escribir cargo/push. Dos requests concurrentes (doble-submit fuera de plazo): el segundo aborta por serialización, reintenta, ve la fecha ya pusheada → 409. NO se puede duplicar el cargo ni pushear la fecha dos veces. El cargo (tx.cargo.create :436) está dentro de la TX. Status de error propaga bien vía `res.status(e.status || 500)` (:503) — 404/403/400/409 viajan correctos.
- Auto-confirmación NO nace pagada: ninguno de los 3 POST (jugador reservas.js:347, admin :717, TF jugador turnos-fijos.js:189) setea pagado. Reserva.pagado @default(false) (schema :192). El @default(true) de schema :390 es el modelo de inscripción de torneo, no Reserva. Los únicos pagado:true son endpoints de cobro explícito (reservas.js:951/:1005, cargos.js:234). El cobro es aparte de la acción de reservar. Correcto.

Sin verificar (límites honestos): no corrí el backend ni reproduje concurrencia real con curl (cierre por trazado + lógica de isolation); sigue pendiente el índice único parcial en DB (canchaId+dia/fecha+horaInicio para estados activos) como red de seguridad defense-in-depth — deuda conocida de auditorías previas, hoy la única defensa es el Serializable a nivel app, correctamente aplicado en TODOS los caminos. No re-validé dinámicamente la aritmética UTC-3 del cargo fuera de plazo (solo lectura).

Veredicto: no logré colar ninguna grieta. El bloque está apto para merge/demo en lo que respecta a doble-booking y plata.


## 2026-06-20 · BLOQUE "automatización de turnos" (branch automatizacion-turnos) — gate de merge · Veredicto: APTO con observaciones (ningún bloqueante)

Archivos leídos: `backend/src/lib/autoConfirma.js`, `backend/src/lib/serializable.js`, `backend/src/lib/deudas.js`, `backend/src/routes/reservas.js` (completo), `backend/src/routes/turnos-fijos.js` (completo), `backend/src/routes/clubs.js` (ocupadasAhora), `prisma/schema.prisma` (enums estado), `frontend/src/store/notificacionesStore.js`, `frontend/src/components/ui/Navbar.jsx` (formatNotif), `frontend/src/pages/ReservasPage.jsx` (panel notif), `frontend/src/pages/PlayerReservasPage.jsx` (handleConfirmar), `frontend/src/pages/PlayerTurnosFijosPage.jsx` (ausencia/cancelar), `frontend/src/pages/PlayerMisReservasPage.jsx` (cancelar).

Hallazgos (ninguno crítico):
- 🟡 [Probable] Auto-liberación de ausencia sin transacción + sin guard de concurrencia. `turnos-fijos.js` POST /:id/ausencia: el check de duplicado (línea 415) y las escrituras (cargo create :434, push diasAusentes :463, cancelar reserva :473-478) NO están en transacción. Doble-submit concurrente del mismo jugador podría pasar ambos el check antes de escribir → fecha pusheada dos veces + cargo duplicado (si fuera de plazo). Mitigado por UI (setEnviando + disabled={enviando} en ModalAusencia :147), no por backend. No genera doble-booking. Sugerencia: envolver en runSerializable o re-leer diasAusentes dentro de TX.
- 🔵 [Seguro] handleConfirmarAusencia (PlayerTurnosFijosPage :338) traga el error del backend en catch y muestra toast de éxito + registra ausencia local. Si el backend rechaza (ej. 409), el front miente "Ausencia registrada"; se corrige en el próximo fetch. Cosmético.
- 🔵 [Suposición] Fallback de dev: addReserva/addTurnoFijoFromApi se ejecutan aun sin backend (canchaDBId/token falsy). En prod siempre hay token, sin impacto. No verifiqué que canchaDBId siempre exista al confirmar.

Verificado OK (lo que de verdad cubrí):
- Anti doble-booking: TODOS los caminos de creación/confirmación corren bajo runSerializable con catch→409 — reservas POST jugador (:327), POST profesor (:433), PATCH profesor edit (:544), POST admin clase-profesor (:604), POST admin reserva/TF (:688), PATCH reserva confirmar (:833); turnos-fijos POST jugador (:171) y PATCH confirmar (:290). serializable.js correcto (Serializable + reintento P2034/40001).
- Auto-confirmación: clubAutoConfirma default true. Estados correctos por modelo: reserva eventual nace 'confirmada', TF nace 'confirmado' (schema.prisma :190/:220 lo confirma). Si flag OFF → 'pendiente' + notif manual (nueva_reserva/solicitud_turno_fijo). Decisión de producto documentada en autoConfirma.js.
- Fixes medianoche: todos los toMin/aMin(horaFin) que comparan rangos tienen guard `=== '00:00' ? 1440`. Verificado en turnos-fijos (:154,:174,:180,:274,:297,:312), deudas.js (:25), clubs.js ocupadasAhora (:71), duración 90min (:154). Los overlaps()/rangoMin() de reservas.js son cross-midnight aware por diseño. No quedó ningún horaFin crudo en comparación de rangos (los demás son selects/data de notif).
- Multi-tenant: clubId siempre de req.user.clubId en todos los queries de negocio; el body trae clubId pero se ignora (reservas POST :295). Sin fuga de tenant.
- Bloqueo baja TF con deuda: intacto. DELETE jugador (:363→409) y PATCH estado inactivo admin (:252→409) cuentan cargos pendientes. No se automatizó.
- Bloqueo admin sobre TF confirmado salvo día liberado (POST /admin :697-715) + RN-51 con overlaps cross-midnight (:739-750), aborta con 409 sin saltear en silencio.
- Notificaciones: normBackend mapea canchaNombre→cancha, horaInicio→inicio, horaFin→fin, dia, fecha, precio, turnoFijoId — coincide con el data que arma el backend para los 3 tipos nuevos. Render explícito de reserva_autoconfirmada/turno_fijo_autoconfirmado/turno_liberado_auto en ReservasPage (:2433-2461) y Navbar formatNotif (:11-16). El fallback de ReservasPage (:2462) excluye los 3 nuevos → NO caen en fila en blanco. notifFiltradas es blacklist, los deja pasar. Navbar default muestra tipo legible (no blanco).
- Notif fire-and-forget: todas con .catch(() => {}) o .catch(console.error), fuera de la TX. No rompen el flujo.
- Doble-submit + commit-timing: PlayerReservasPage handleConfirmar (:462) guard `|| submitting` + setSubmitting + disabled={submitting} (:1057); pantalla de éxito se setea recién tras la respuesta de la API (:552). PlayerMisReservasPage (cancelando+disabled, toast.error en catch) y PlayerTurnosFijosPage (enviando/cancelando/retirandoId) OK.
- Pantalla de éxito distingue confirmado (auto) vs enviado (pendiente) leyendo turno.estado/reserva.estado del backend (:480,:514). Correcto.

Sin verificar (límites honestos): no corrí el backend ni reproduje concurrencia real con curl (cierre por trazado de código); no validé dinámicamente el cargo por ausencia fuera de plazo (solo leí la aritmética UTC-3); no re-audité el panel admin de aprobación manual con flag OFF end-to-end; sigue pendiente el índice único parcial en DB (canchaId+dia/fecha+horaInicio) como red de seguridad — deuda conocida de auditorías previas, no introducida por este bloque.

Veredicto de merge: APTO. El bloque cumple las reglas de negocio centrales (slots 1.5h, Serializable en todos los caminos, tenant por JWT, estados correctos, medianoche, baja bloqueada por deuda, notif sin filas en blanco). El único hallazgo accionable (🟡 ausencia sin TX) es un caso borde de concurrencia mitigado por UI; recomiendo abrirlo como issue de hardening post-merge, no bloquea.


## 2026-06-20 · CIERRE — Reservas + Turnos fijos: verificación de fixes del hallazgo CRÍTICO · Veredicto: APTO para cliente

Re-auditoría de seguimiento. Verifiqué leyendo el código real que los 3 fixes están aplicados:

- 🔴→✅ [Seguro] Turnos fijos sin Serializable — CORREGIDO. `turnos-fijos.js:3` importa `runSerializable` de `../lib/serializable.js`. AMBOS caminos lo usan ahora:
  - POST `/` (solicitud jugador): `turnos-fijos.js:162` envuelve el chequeo de solapamiento RN-51 + `create` en `runSerializable`, con `throw {status:409}` (línea 172) y catch→409 (línea 217).
  - PATCH `/:id/estado` confirmar: `turnos-fijos.js:272` envuelve la re-verificación (TF solapado + reserva eventual/clase conflictiva en las próximas 8 ocurrencias) + `update` en `runSerializable`, throws 409 (líneas 282, 298), catch→409 (línea 327). La aritmética de fechas queda fuera de la TX (correcto, no toca DB).
- 🟡→✅ [Seguro] Editar clase profesor — CORREGIDO. `reservas.js` PATCH `/profesor/:id` (línea 528) envuelve los 3 chequeos de conflicto (reserva en cancha, turno fijo activo, otra clase del mismo profesor) + `update` en `runSerializable`, todos con `throw {status:409}` (líneas 534, 542, 549) y catch→409 (línea 561).
- ✅ [Seguro] DRY — `lib/serializable.js` extraído: `runSerializable(fn, retries=2)` corre `$transaction` con `isolationLevel: Serializable`, reintenta hasta 2 veces ante P2034/40001, re-lanza el resto. Importado por `reservas.js:3` y `turnos-fijos.js:3`. Los comentarios que afirmaban falsamente que `$transaction` plano prevenía el race fueron reemplazados por la explicación correcta (READ COMMITTED no impide TOCTOU; Serializable aborta + reintenta).

Validación e2e reportada por el dev (no la corrí yo en esta pasada): 2 solicitudes paralelas al mismo slot → una 201, otra 409, queda 1 solo TF. Consistente con el trazado de código.

Pendiente (NO bloqueante, mejora defense-in-depth): índice único parcial en DB sobre Reserva/TurnoFijo (canchaId+dia/fecha+horaInicio para estados activos) como red de seguridad ante un eventual bug que saltee el camino Serializable. Postergado al deploy porque requiere índice parcial + dedup previo de filas existentes. Hoy la única defensa es el isolation a nivel app, que está correctamente aplicado en todos los caminos auditados.

Veredicto: el hallazgo CRÍTICO que dejaba NO APTO el flujo está cerrado. APTO para mostrar a un dueño de club.

Sin verificar en esta pasada: no corrí el backend ni reproduje concurrencia real con curl (cierre por trazado de código); no re-audité UI (PlayerTurnosFijosPage / panel admin de aprobación); el índice de DB queda como deuda conocida.

## 2026-06-20 · Reservas eventuales + Turnos fijos (punta a punta) · Veredicto: NO APTO (por 1 hallazgo CRÍTICO acotado)

Archivos leídos: `backend/src/routes/reservas.js`, `backend/src/routes/turnos-fijos.js`, `prisma/schema.prisma` (modelos Reserva/TurnoFijo), `frontend/src/pages/PlayerReservasPage.jsx`, `frontend/src/store/reservasStore.js`, `reglas-negocio.md`.

Hallazgos:
- 🔴 [Seguro] Aprobación de turno fijo y solicitud de TF de jugador usan `prisma.$transaction` SIN `isolationLevel: Serializable` ni reintento P2034. turnos-fijos.js:160 (POST jugador) y turnos-fijos.js:269 (PATCH /:id/estado confirmar). No hay unique constraint en DB (schema.prisma:211-234 sin @@unique sobre canchaId+dia+horaInicio), así que el isolation es la ÚNICA defensa. Ventana TOCTOU real → doble-booking de TF / dos TF confirmados sobre el mismo slot bajo concurrencia. Viola la regla "todo camino que crea/confirma reservas usa runSerializable". reservas.js SÍ lo cumple en todos sus paths.
- 🟡 [Probable] reservas.js POST /admin crea Reserva+TurnoFijo atómico bajo runSerializable, pero el chequeo anti-solapamiento de TF dentro no re-valida RN-51 multi-jugador con la misma robustez; menor.
- 🔵 Nota: clubId viaja en el body de POST /reservas (frontend:499) pero el backend lo ignora y usa req.user.clubId (reservas.js:312). Sin fuga de tenant. OK.

OK verificado: slots 1.5h (frontend genera en pasos de 90', backend valida duracion===90 en TF; reserva eventual confía en horaFin del front); estados correctos (reserva jugador y TF nacen `pendiente`, alineado con RN-08/RN-14 del proyecto — OJO: el prompt del agente decía "reserva normal = confirmada inmediata" pero la fuente de verdad del proyecto es pendiente→aprobación admin); runSerializable correcto en TODOS los paths de reservas.js con catch→409; multi-tenant por req.user.clubId en todos los queries; doble-submit guard OK (submitting + disabled + guard en handler); precio fijo (sin recargoPico); baja TF bloqueada con deuda (turnos-fijos.js:233 y :342 → 409); reservasStore en memoria, sin localStorage de negocio.

Sin verificar (límites): no corrí el backend ni probé concurrencia real con curl (el hallazgo CRÍTICO es por trazado de código + ausencia de constraint, no dinámico); no leí lib/deudas.js en detalle (el bloqueo por deuda en TF usa cargo.count inline, no esa lib); no audité PlayerTurnosFijosPage ni el panel admin de aprobación (UI); no revisé generarSlots contra cruces de medianoche exhaustivamente.
